# 이 저장소에서 일할 때

## 말하는 방식 — 이게 첫 번째다

**주식·매매 용어는 언제나 그 자리에서 풀어 쓴다.** 이 저장소의 주인은
트레이딩과 파이썬을 배우는 중이고, "슬리피지가 뭐야"라고 물어야 하는 상황을
만들지 않는 것이 이쪽 일이다.

- 전문용어를 처음 쓸 때 **한 줄로 뜻을 붙인다** — "슬리피지(사겠다고 판단한
  값과 실제로 사진 값의 차이)"
- 뜻만 말고 **그래서 그걸 보면 뭘 판단하는지**까지 말한다. "MDD는 최대낙폭"
  만으로는 −30%를 보고 무엇을 해야 할지 모른다.
- 숫자를 낼 때는 **어느 조건에서 나온 것인지**를 붙인다(유니버스·기간·비용
  가정). 조건 없는 숫자는 나중에 검증할 수가 없다.
- 영어 약어는 한글 뜻을 같이 준다. PF → 손익비, MDD → 최대낙폭.

용어 사전이 이미 있다: `src/muwon/dashboard/glossary.py` (70개, 대시보드와
`docs/용어사전.md`가 이걸 함께 쓴다). **새 용어를 설명했으면 사전에도
넣는다** — 화면과 대화가 같은 말을 쓰게.

### 보고할 때는 평범한 업무 보고체로 쓴다

규칙을 외우려 하지 말고 아래 문단을 본보기로 삼는다. 보고, 커밋 메시지,
사용자에게 가는 알림 문구가 전부 이 말투다.

> 오늘 09:05 매수가 실행되지 않았습니다.
>
> 원인은 두 가지입니다. 첫째, DB에 저장해 둔 KIS 토큰이 이미 만료된
> 상태였습니다. 둘째, 그 만료 응답을 매수가능수량 0으로 잘못 읽었습니다.
> 그래서 승인된 두 종목이 모두 걸러졌습니다. 계좌 잔고는 정상이었습니다.
>
> 토큰이 만료되면 재발급 후 한 번 재시도하도록 수정했습니다. 응답 파싱도
> 함께 고쳐서, 조회에 실패한 경우와 실제로 0주인 경우를 구분합니다.
> 테스트 3건을 추가했고 전체 902건이 통과합니다.
>
> 실제 계좌에서의 확인은 내일 09:05 실행 이후에 가능합니다.

이 문단이 지키는 것:

- 존댓말 평서문. 한 문장에 한 가지만 담는다.
- 결함은 결함이라고 부른다. 비유를 새로 만들지 않는다.
- 줄표(—)를 쓰지 않는다. 쉼표와 마침표로 끊는다.
- 굵게 표시를 쓰지 않는다. 중요한 것은 앞에 놓는 것으로 드러낸다.
- 개발 용어는 그대로 쓴다. 토큰, 파싱, 재시도, 테스트, 캐시.
- 확인하지 못한 것은 확인하지 못했다고 적는다.

**긴 세션에서 특히 흐트러진다.** 앞에 쓴 내 문장을 다음 문장의 본보기로
삼기 때문이다. 사고 보고서 말투로 한 번 들어가면 줄표와 비유가 쌓인다.
2026-08-26에 커밋 12개(338줄)에서 줄표가 46개 나왔다. 길어진다 싶으면
위 문단을 다시 읽고 거기에 맞춘다.

코드 안의 한글 이름(`열쇠`, `창구`, `곁말`)은 이 저장소가 이미 쓰는 것이라
그대로 둔다. 사람에게 말할 때만 일반적인 말로 바꿔 부른다.

### 결론을 먼저 쓴다 (2026-08-31에 지적받음)

답의 순서는 결론, 근거, 이슈다.

1. **결론.** 물어본 것에 대한 답을 첫 문단에 쓴다. 한두 문장이면 된다.
2. **근거.** 그렇게 판단한 이유와 숫자. 표가 있으면 여기에 놓는다.
3. **이슈.** 걸리는 것, 확인하지 못한 것, 다음에 할 것.

과정을 순서대로 늘어놓고 마지막에 결론을 놓지 않는다. 읽는 사람은 결론을
보려고 여는 것이고, 근거는 결론에 동의가 안 될 때 본다.

### 실제로 쓰는 문장인지 확인하고 쓴다 (2026-08-31에 지적받음)

**쓰기 전에 이 문장을 사람이 실제로 이렇게 말하는지 한 번 본다.** 뜻이
통한다고 자연스러운 것은 아니다. 아래는 내가 실제로 쓴 어색한 문장들이다.

| 쓴 것 | 고칠 것 |
|---|---|
| 실거래는 그 목록으로 안 돕니다 | 실거래는 그 목록을 쓰지 않습니다 |
| 지금 도는 매매의 성적이 아닙니다 | 지금 하는 매매의 성적이 아닙니다 |
| 서로 다른 운동장에서 돕니다 | 서로 다른 종목 목록을 봅니다 |
| 이건 제가 앞서 드린 설명이 부정확했습니다 | 앞서 드린 설명이 틀렸습니다 |
| 표본이 적은 자리입니다 | 표본이 적습니다 |

**특히 조심할 것.** 워크플로나 전략에 "돌다"를 쓰면 대부분 어색해진다.
실행한다, 계산한다, 쓴다로 바꾼다. "~하는 자리다"도 습관처럼 붙는다.
꼭 필요할 때만 쓴다. 비유는 아예 새로 만들지 않는다.

### 저장소 안에서만 쓰는 말을 대화에 쓰지 않는다 (2026-08-31에 지적받음)

이 저장소는 코드와 화면에 한글 이름을 많이 쓴다. 그 이름들을 사람에게
말할 때 그대로 가져다 쓰면 못 알아듣는다. 흔한 말로 바꿔서 말한다.

| 코드·화면에서 쓰는 말 | 사람에게 말할 때 |
|---|---|
| 토막 | 구간 |
| 쪼개다 | 나누다 |
| 견주다 | 비교하다 |
| 돌려 보다, 돌리다 | 테스트하다, 실행하다 |
| 전략을 걸다 | 전략을 설정하다 |
| 창구 | n8n 연결 |
| 열쇠 | 접속 키 |
| 곁말 | 보조 설명 |
| 문턱 | 최소 기준 |
| 유니버스 | 매매 대상 종목 |
| 성적표 | 전략 평가 결과 |
| 기초전략 / 세부전략 | 공통 매매 규칙 / 개별 전략 |
| 낙폭 | 하락폭 |
| 손익비 | 이익 대비 손실 비율 |
| 노출 | 종목을 보유한 기간 비율 |

**어려운 말을 아예 안 쓰는 것이 목표가 아니다.** 필요하면 쓰되 그 자리에서
뜻을 붙인다. 목표는 읽는 사람이 사전을 찾지 않고 한 번에 이해하는 것이다.

**긴 답일수록 이 규칙이 먼저 무너진다.** 내가 앞에 쓴 문장을 다음 문장의
본보기로 삼기 때문이다. 답이 길어진다 싶으면 이 표를 다시 본다.

### 영어를 직역하지 않고, 피동형을 남발하지 않는다 (2026-08-31에 지적받음)

내가 어색한 문장을 쓰는 이유가 둘이다.

**하나, 영어 표현을 그대로 옮긴다.** 뜻은 통하지만 한국어로는 안 쓰는 말이
된다. "질문이 열려 있습니다"가 그 예다. open question을 직역한 것이고,
한국어로는 "아직 답을 못 드렸습니다"라고 쓴다.

**둘, 필요 없는 피동형을 붙인다.** 능동으로 쓸 수 있는데 "-됩니다",
"-립니다", "-힙니다"를 습관처럼 붙인다. **피동형을 쓰기 전에 능동으로
바꿔 보고, 뜻이 같으면 능동으로 쓴다.**

| 쓴 것 | 고칠 것 |
|---|---|
| 질문이 열려 있습니다 | 아직 답을 못 드렸습니다 |
| 믿을 수 있는지 판단됩니다 | 판단할 수 있습니다 |
| 이 숫자로 확인됩니다 | 확인할 수 있습니다 |
| 구간 문제로 읽힙니다 | 구간 문제로 보입니다 |
| 진짜일 때도 안 읽힙니다 | 정작 중요할 때도 넘기게 됩니다 |
| 무엇이 문제인지 갈립니다 | 무엇이 문제인지 구분할 수 있습니다 |
| 표에 잡힙니다 | 표에서 확인할 수 있습니다 |

피동형이 맞는 자리도 있다. 행위자가 없거나 중요하지 않을 때다.
"주문이 거절됐습니다"는 그대로 둔다.

### 단어만 한글로 바꾸면 한국어가 아니다 (2026-08-31에 또 지적받음)

같은 지적을 네 번째 받았다. 앞의 규칙들을 지켜도 **문장 구조가 영어면**
읽는 사람은 여전히 못 알아듣는다. 단어를 하나씩 옮기지 말고, 한국어로
그 뜻을 어떻게 말하는지 생각한 다음 쓴다.

| 쓴 것 | 왜 이상한가 | 고칠 것 |
|---|---|---|
| 시가총액으로 무게를 주면 | give weight를 직역 | 시가총액이 큰 종목에 비중을 더 두면 |
| 세 단계입니다 | It's three steps | 계산은 이렇게 합니다 |
| 절대 수익률이 아니라 차이입니다 | absolute return 직역 | 실제로 얼마나 올랐는지는 이 값만으로 알 수 없습니다 |
| 이 숫자로 지금 무엇을 하나 | What do we do with this | 이 값을 어디에 쓰나 |
| 줄만 세우고 거르지 않습니다 | 저장소 안에서 쓰는 말 | 순서만 매기고 걸러 내지는 않습니다 |

**설명할 때는 명사를 늘어놓지 말고 문장으로 잇는다.** 짧은 명사구를
줄줄이 쓰면 번역문처럼 읽힌다. 주어와 서술어가 있는 문장으로 쓰고,
문장끼리 그래서·그다음·대신 같은 말로 이어 준다.

**표와 목록을 습관처럼 쓰지 않는다.** 표는 숫자를 견줄 때만 쓴다.
설명을 표로 만들면 문장을 안 쓰게 되고, 그러면 번역문 티가 더 난다.

### 화면에 나가는 글은 대화보다 한 단계 더 딱딱하게 쓴다 (2026-09-01)

대시보드 문구 지침을 받았다. 원칙은 하나다. **쉽게 쓰되 유치하게 쓰지
않는다.** 쉽게 쓰려다 구어체가 되면 돈을 다루는 화면의 신뢰가 떨어진다.

지적받은 표현과 고친 것이다.

| 쓴 것 | 고칠 것 |
|---|---|
| 잽니다, 재 봅니다 | 계산합니다, 검증합니다 |
| 열쇠 | 인증 키 |
| 창구 | n8n 연결 |
| 아무 일도 일어나지 않습니다 | 주문이 발생하지 않습니다 |
| 돈이 나가는 결정이라 | 실제 주문에 직접 영향을 주므로 |
| 사는 쪽으로 기우는 기본값 | (설명을 다시 쓴다) |
| 한 주도 안 샀습니다 | 매수가 발생하지 않았습니다 |
| 제일 나빴던 토막 | 최악의 기간, 성과가 가장 낮은 구간 |
| 켜짐 / 꺼짐 | 동작 중 / 중지 |
| 표본이 적습니다 | 표본 부족 |

**문구를 고치기 전에 코드부터 연다.** 이번에 문구와 코드가 어긋난 곳이
열 군데 나왔다. 문구를 다듬는 일이 아니라 어긋난 곳을 찾는 일이 먼저다.
어긋나면 코드에 맞춰 문구를 고치고, 코드가 틀렸으면 그렇다고 보고한다.

**바뀔 수 있는 값을 설명문에 박아 넣지 않는다.** 09:05, 5~15분, 20건,
5년, 2일, 30% 같은 것이다. 일정표처럼 그 값 자체가 내용인 자리는 예외다.
표본 20건 문턱은 `dashboard/app.js`의 `표본문턱`과 `analysis/period_check.py`
두 곳에 있고, 어긋나면 같은 줄이 표마다 다르게 표시된다.

**보안을 과장하지 않는다.** "절대 외부로 나가지 않습니다"는 확인한 것만
쓴다. 인증 키는 조회할 때마다 서버로 보낸다. 브라우저 밖으로 안 나간다고
적으면 거짓말이다.

**투자 판단으로 읽히는 말을 쓰지 않는다.** 좋은 전략, 안전한 전략,
추천 같은 말이다. 숫자에서 바로 나오는 사실만 적는다.

**백테스트와 실제 주문과 실제 체결을 한 표에 두지 않는다.** 셋은 다른
것이고, 섞이면 번 적 없는 돈을 번 것으로 읽는다.

**긴 세션에서 이 규칙이 먼저 무너진다.** 앞에 쓴 내 문장을 다음 문장의
본보기로 삼기 때문이다. 화면 글을 고칠 때는 이 절을 다시 읽는다.

## 사람 손을 최소화한다 — 이것도 첫 번째다

**내가 확인할 수 있는 것을 사람에게 묻지 않는다.** "Published인지 봐 주세요",
"Executions에 기록이 있나요", "그 값이 뭐예요" — 전부 도구로 직접 확인할 수
있는 것들이다. 물어보면 그 사람은 화면을 찾아 헤매고, 찾은 뒤에도 그걸
받아 적어 나에게 전해야 한다. 그 왕복이 곧 일이 안 되게 만드는 이유다.

- **확인은 내가 한다** — n8n MCP, GitHub Actions API, 워크플로 로그, 시트,
  DB. 닿을 수 있는 곳이면 물어보지 말고 본다.
- **바꾸는 것도 내가 할 수 있으면 내가 한다.** 워크플로 Publish, 노드 켜기,
  설정 고치기. 사람 손이 필요한 것만 남긴다.
- **남길 때는 정확히 무엇을 어디서 누르는지까지 적는다.** "노드를 켜세요"는
  안내가 아니다. 어느 화면, 어느 버튼인지 모르면 못 한다.
- **정말 사람만 할 수 있는 것**은 셋뿐이다: 비밀값 발급(토큰·API 키),
  돈이 나가는 결정(킬스위치를 켤지), 그리고 취향이 갈리는 선택.
- 못 하는 것을 시킬 때는 **왜 내가 못 하는지**를 한 줄로 밝힌다.

## 확인하지 않고 단정하지 않는다 (2026-08-31에 두 번 틀림)

**이 저장소에 대해 사실을 말하기 전에 코드나 자료를 연다.** 기억이나
추론으로 말하면 틀린다. 하루에 두 번 틀렸다.

- "실거래가 시가총액 30종목 목록으로 돈다"고 말했다. 실제로는
  `propose_buys.py`가 구글 시트의 종목 탭을 읽는다. 파일을 안 열고 말했다.
- "주문 기록이 0건"이라고 인수인계서에 썼다. 실제로는 주문 8건, 완결된
  매매 3건, 보유 3종목이 있었다. 상태 DB를 안 읽고 CLAUDE.md 문장을
  그대로 옮겼다.

둘 다 확인에 1분도 안 걸리는 것이었다. **긴 세션 탓이 아니다.**

지킬 것은 셋이다.

1. 숫자나 동작을 말하기 전에 그 값이 어디서 나오는지 열어 본다.
   설정은 시트, 기록은 상태 DB, 동작은 스크립트다.
2. 이 저장소의 다른 문서에 적힌 문장을 옮길 때는 **지금도 맞는지
   확인한다.** CLAUDE.md의 "지금 상태"는 2026-08-19자다.
3. 확인 못 한 것은 "확인하지 못했습니다"라고 적는다. 그럴듯하게 적는
   것보다 모른다고 적는 쪽이 훨씬 낫다.

## 결과를 보고할 때

- **틀린 가설도 그대로 보고한다.** 이 저장소는 기각된 가설을 자산으로
  취급한다(`docs/가설기록.json`, 구글 시트). 같은 걸 두 번 시험하지 않으려면
  실패가 보여야 한다.
- 잘 안 된 것을 좋게 포장하지 않는다. "최악 구간이 나빠졌다"를 "평균은
  좋아졌다"로 덮지 않는다.
- **1순위 판단 기준은 평균이 아니라 가장 나빴던 구간이다.** 평균이 아무리
  높아도 한 해에 30% 잃으면 대부분 중간에 그만두고, 그러면 평균은 받아 보지도
  못한다.

## 이 저장소에서 반복해서 다시 알게 되는 것들

- **로컬 venv를 쓴다.** 시스템 `python3`에는 pandas가 없다 →
  `/home/user/muwon406/.venv/bin/python`
- **대시보드는 브라우저로 직접 확인한다.** 테스트와 ruff를 통과하고도 화면이
  죽은 적이 여러 번 있다(같은 키의 폼 두 개, HTML 태그 노출 등).
  Playwright + `/opt/pw-browsers/chromium`.
  - **킬스위치 단추는 `confirm()`을 띄운다.** headless에서 그냥 클릭하면 거기서
    멈춘다. `page.on("dialog", ...)`로 받아 준다. 안 받으면 확인이 통째로 시간
    초과로 끝나는데, 그 실패가 "화면이 느리다"처럼 보여서 원인을 한참 못 찾는다.
  - **화면에 뜰 수 있는 상태는 셋이다** — 진짜 자료 / 아직 비었음 / 아직 연결
    안 됨. 뒤의 둘을 같은 빈 표로 그리면 "아직 아무것도 안 샀다"와 "산 것을 볼
    수가 없다"가 구별되지 않는다. 뒤쪽이 훨씬 나쁘다.
- **폰 폭(390px)으로도 본다.** PC 폭에서만 보면 카드가 잘리고 제목이 두 줄로
  넘치는 걸 못 잡는다.
- **workflow_dispatch의 `type: boolean` 입력에 문자열을 넘기면 안 된다.**
  `"false"`가 참으로 읽혀서 진짜 올리려던 실행이 조용히 dry-run으로 끝났다
  (2026-08-26). 초록불인데 아무것도 안 올라간다. JSON 불리언 `false`를
  넘길 것. 그리고 스크립트가 **어느 쪽으로 도는지 맨 위에 찍게** 해 뒀다.
- **셸 변수 이름에도 한글을 쓰면 안 된다.** 워크플로 `run:` 안에서
  `인자=""`를 썼더니 bash가 `command not found`로 죽었다(2026-09-01).
  Actions 표현식만 그런 줄 알았는데 셸도 같다. 파이썬 argparse 인자 이름은
  한글이어도 된다(`--최소운용일`) — 셸은 그걸 글자로만 넘긴다. 막히는 것은
  **대입문의 왼쪽**이다. `tests/test_workflow_locks.py`가 훑는다.
- **GitHub Actions 표현식에 한글 식별자를 쓰면 안 된다.** `id: 배포`와
  `steps.배포.outputs`로 워크플로가 0초 만에 죽었고(로그도 없다),
  `workflow_dispatch` 입력 이름을 `기간`으로 두었다가 dispatch가 422로
  거절됐다(2026-08-28). 파일 전체가 파싱에 실패하는 방식이라 무엇이 틀렸는지
  안 보인다. **식별자는 영문, 사람이 읽는 name·description·값은 한글**이다.
  `tests/test_workflow_locks.py`가 워크플로 전부를 훑어 고정한다.
- **GitHub Actions 실행 목록 응답은 토큰 한도를 넘는다.** 파일로 저장된 뒤
  python/json으로 파싱할 것. `per_page`를 줘도 무시하고 전부 돌려준다
  (2026-08-26에 29건이 통째로 왔다). `actions_get`으로 하나씩 보는 편이 낫다.
- **실험 워크플로의 `fill_timing` 기본값은 `close`다.** 성적표는 `open`
  (실거래와 같은 규칙)으로 잰 숫자다. 기본값으로 돌려서 성적표에 넣으면
  조건이 다른 숫자를 섞게 된다.
- **유니버스 스냅샷은 주마다 바뀐다.** 같은 조건으로 다시 재도 숫자가 달라진다.
  성적표는 항상 한 실행에서 나온 숫자만 담는다.
- 실험 결과는 Actions 로그(만료됨)와 아티팩트에 남는다. 판단은
  `docs/전략엔진_설계안.md`에, 성적은 `docs/전략평가.json`에.
- **조용히 성공한 척하는 실패가 제일 비싸다.** 이 저장소에서 이미 세 번
  나왔다 — `| tee`가 종료 코드를 삼킨 것, `try/except`가 텔레그램 실패를
  삼킨 것(초록불 8번, 리포트는 한 번도 안 옴), `if: always()` 업로드가
  진짜 오류를 로그 아래로 밀어낸 것. 새 코드를 쓸 때마다 물을 것:
  **이게 실패하면 무엇이 빨개지나?** 아무것도 안 빨개지면 실패를 숨기는
  코드다. (`docs/사고_2026-08-25_조용한실패.md`)
- **초록불도 가끔 열어 본다.** 실패는 언젠가 보지만 "성공했는데 아무 일도
  안 한" 실행은 안 본다. 특히 오래 안 건드린 워크플로.
- **분봉은 그날 안에만 받을 수 있다.** 수집이 실패한 날은 그날 저녁에 손으로
  다시 돌리면 구제된다 — 장 마감(15:30) 뒤에도 당일치는 나온다(16:43에
  100% 확인). 다음 날이면 영영 없다.

## 다음 회차가 확인할 것 (2026-09-01 저녁에 적음)

**전략이 내일 아침 바뀐다.** 사람이 2026-09-01 15:36에 `volatility_breakout_k05`
(변동성 돌파)로 확정했다. `strategy-apply.yml`이 09-02 08:20에 반영한다.
지금 걸린 것은 `volume_surge_5d_ma20`이다.

확인할 것이 셋이다. 하나라도 어긋나면 그날 매매가 예상과 달라진다.

1. **08:20 전략 변경 반영이 초록불인가.** 그리고 로그에 "전략을 바꿨습니다"가
   찍혔는가. 막힘으로 끝났으면 까닭이 텔레그램으로 간다.
2. **08:30 매수 후보가 새 전략으로 나왔는가.** 시트 `승인대기` 탭의 전략 칸이
   `volatility_breakout_k05`여야 한다. 여기가 옛 전략이면 08:20이 안 돈 것이다.
3. **보유 중인 3종목이 새 규칙으로 팔리는가.** 청산 판단은 들어올 때의 전략이
   아니라 지금 걸린 전략을 본다. 09:05 회차에서 매도가 늘 수 있다.

같이 볼 것.

- **17:50 검토가 자동으로 처음 돈 것이 09-01 저녁이다.** 그전까지는 손으로
  실행했다. 첫 자동 회차가 초록불인지 한 번 열어 본다.
- **승인 버튼이 한 단계로 바뀌었다.** 09-01 저녁 메시지부터 적용된다. 한 번
  누르면 예약, 같은 것을 다시 누르면 취소다. 설계안 §40.
- **그림자 추적의 첫 숫자는 10월 초에 나온다.** 09-01에 남긴 12줄이 30일 뒤에
  계산된다. 그전까지 화면의 그 표는 비어 있는 것이 정상이다. 설계안 §39.
- **09-01 15:15 손절 감시 한 회차가 실패했다.** 증권사 응답 시간 초과다.
  5분 뒤 회차는 성공했다. 같은 실패가 되풀이되면 그때 본다.

## 지금 상태 (2026-08-19)

- **모의투자 자동 실행은 꺼져 있다.** `paper-trading.yml`의 schedule을
  주석 처리했고, 그 상태를 테스트가 고정하고 있다(다시 켜면 테스트가 실패해서
  "의도한 변경인가"를 한 번 묻는다).
- **모의투자로는 이미 사고팔았다.** 2026-08-31 기준 주문 8건, 완결된 매매
  3건, 보유 3종목, 총자산 1,017만원이다. "주문 기록이 0건"이라고 적으면
  안 된다(2026-08-31에 인수인계서에 그렇게 잘못 적었다). 0건인 것은
  **실제 돈이 나가는 거래**다.
- **슬리피지는 아직 못 쟀다.** 기준가와 체결가가 둘 다 남아 있지만,
  기준가는 전날 종가이고 체결은 다음 날 시가라 밤사이 변동과 섞여 있다.
  둘을 나누려면 주문 낸 시각의 호가를 따로 기록해야 하는데 안 하고 있다.
  지금 모든 백테스트는 슬리피지를 0으로 놓고 계산한다.
- 전략 27개 중 5년 내내 플러스는 다섯. 조합(AND/OR)·익절·진입 상한은 전부
  재보고 기각. 지금 걸린 것은 `volume_surge_5d_ma20` 하나다.
- **매수와 매도를 따로 걸 수 있다**(`SplitStrategy`). 조합 12개를 재 봤고
  둘이 지금보다 최악의 해가 낫다 — `ma_rsi_v1>volume_surge_5d_ma20`(최악
  +10.6%, 평균은 8.9%p 낮다)과 `volume_surge_5d>macd_cross`(최악 +4.3%,
  평균 +38.0%, 대신 시간 청산이 사라진다). **아직 안 걸었다** — 돈이
  나가는 결정이다. 설계안 §31.

## 기간별 검증 (2026-08-28)

- **대시보드 '기간 검증' 탭에서 지금 걸린 전략을 1주·1개월·3개월·12개월·
  5년에 돌려 본다.** 단추 → n8n 창구 → `period-check.yml` → 시트 `기간검증`
  탭 → 화면. 주문은 안 나간다. 상태 DB도 안 고쳐서 `state-write` 그룹에
  안 묶었다.
- **1주와 1개월의 숫자는 판단에 못 쓴다(2026-08-28에 더함).** 1주는 거래가
  0건인 회차가 흔하고, 그럴 때 수익률 0%가 표 맨 위에 온다. 지킨 것이
  아니라 아무 일도 안 한 것이다. **전략 신호도 이 둘을 안 본다** — 3개월·
  12개월·5년만 읽는다. 거래 몇 건으로 신호를 내면 매주 뭔가 뜨고, 매주 뜨는
  것은 진짜일 때도 안 읽힌다.
- **쪼개는 단위는 구간 길이를 따라간다.** 1주는 날, 1개월은 주, 3개월·12개월은
  달, 5년은 해다. 1개월을 달로 쪼개면 토막이 하나뿐이라 '제일 나빴던 달'이
  구간 전체와 같아진다. 주는 ISO 주차로 센다(연말연시에 순서가 안 꼬인다).
- **1주는 달로 안 떨어져서 `기간정의.날수`를 쓴다.** `달수`가 0이면 날수로
  거슬러 올라간다. 새 구간을 더할 때 둘 중 하나만 채운다.
- **성적표와 재는 방법이 다르다.** 성적표는 해마다 따로 재고, 여기서는 한
  구간을 통째로 이어서 잰다. 섞어 쓰면 안 된다.
- **처음 잰 숫자(2026-08-28)**: 3개월 -20.74%(21건, 승률 4.8%),
  12개월 +24.90%(136건), 5년 +154.86%(674건). 최대낙폭은 세 구간 모두
  -22%~-32%. 설계안 §35.
- **거래 수 20건 문턱은 걸러 내는 값이지 믿어도 된다는 값이 아니다.**
  이번 3개월이 21건이라 문턱을 넘었지만 그것으로 판단할 숫자는 아니다.
- **화면에서 매수·매도 전략을 골라 돌릴 수 있다(2026-08-28).** '지금 걸린 것',
  '등록된 전략 전부(27개 견주기)', 개별 전략 중에서 고른다. 결과는 시트
  `기간비교` 탭으로 가고 화면의 '골라서 돌려 본 것' 표에 뜬다. **여기서
  고른 것으로 매매는 안 바뀐다** — 전략을 실제로 바꾸는 것은 워크플로에서만.
- **`${{ github.event.inputs.* }}`를 `run:` 안에 직접 박지 말 것.** 셸 스크립트
  글자 안으로 그대로 치환돼서 `$(...)`가 섞인 값이 명령으로 실행된다.
  `env:`로 넘기고 `"$이름"`으로 쓴다. `period-check.yml`은 고쳤고, 같은 모양이
  **다른 워크플로 17개에 남아 있다**(`tests/test_workflow_locks.py`의
  `아직안고친것` 목록). 그 목록은 줄기만 하도록 고정해 뒀다.
- **이 비교의 절대 수익률은 못 믿는다.** 유니버스가 '지금' 시가총액 상위
  30종목이라 그 목록을 5년 전으로 가져가 돌린다. 살아남은 회사만 보는
  것이라 숫자가 부풀려진다. 편향이 27개 전부에 똑같이 걸리므로 **순위
  비교에만** 쓴다.
- **최근 3개월은 전략 문제가 아니라 구간 문제다(2026-08-28).** 27개 중
  25개가 마이너스다. 지금 걸린 것은 22위 -20.74%. 방어된 쪽은 전부 역추세
  계열이고 노출이 낮다. 설계안 §36.
- **`volume_surge_3d`를 한 번 봐 둘 것.** 5년 최대낙폭 -10.90%로 27개 중
  1위이고(지금 걸린 것은 -32.30%) 수익률은 +126.59%로 10위다. 아직 안
  걸었다 — 돈이 나가는 결정이고, 성적표와 같은 자로 다시 재는 것이 순서다.

## 매일 전략 검토와 변경 승인 (2026-09-01)

원하는 구조가 두 루프다. 하나는 매매고 하나는 전략 검토다.

    08:30 매수 후보 → 사람 승인 → 09:05 매수·매도 → 장중 손절 감시
      → 17:30 체결 정산 → 17:40 기록 → 다음 날 08:30

    17:50 전략 검토 → 텔레그램 버튼 → 사람이 두 번 누름
      → 08:20 반영 → 08:30 새 전략으로 후보 산출

**매매 루프는 원래 다 있었다.** 손댈 것이 없다. 검토 루프가 이번에 붙은 것이다.

- **검토는 구간마다 후보를 따로 낸다.** 1주·1개월·3개월이고, 셋이 다른 전략을
  가리킬 수 있다. 한 구간의 1위를 그대로 걸면 그 구간에 맞춘 것이 된다.
  셋을 같이 보면 답이 모이는지 갈리는지가 보인다.
- **버튼은 한 번 누르면 예약, 같은 것을 다시 누르면 취소다(2026-09-01에
  바꿈).** 처음에는 고름과 확정 두 단계였는데 번거롭다는 지적을 받아 줄였다.
  손가락이 스치는 것에 대한 방어는 반영 시점이 대신한다. 눌러도 그날 안 바뀌고
  다음 거래일 08:20에 반영하므로 저녁 내내 되돌릴 시간이 있다.
- **옛 메시지의 버튼도 계속 받는다.** 두 단계였을 때의 확인 버튼(`c`)과 취소
  버튼(`x`)이 대화방에 남아 있다. 확인 버튼은 지금의 누르기와 똑같이 처리한다.
- **규칙은 `cloud/strategy_approval.py` 한 곳에만 있다.** 버튼을 그리는 곳과
  받는 곳과 반영하는 곳이 전부 이 파일을 지나간다. 세 곳이 각자 DB를 만지면
  규칙이 세 벌이 되고, 셋이 어긋나도 아무것도 안 빨개진다.
- **예약은 동시에 하나만.** 새로 고르면 앞의 것이 취소된다. 둘을 동시에
  예약하면 다음 날 무엇이 반영되는지 알 수 없다.
- **막힌 이유를 반드시 적는다.** 아무 말도 없으면 "볼 것이 없다"와 "계산이
  안 됐다"가 구별되지 않는다. 반영이 막히면 상태를 `막힘`으로 적고 알린다.
- **어느 문장도 "바꿔라"라고 말하지 않는다.** 후보를 보여 주고 판단을 사람에게
  넘긴다. 테스트가 고정한다.
- **63종목 세 구간에 27전략이 약 3분이다.** 2026-09-01 첫 실행에서 2분 53초.
  매일 돌리는 데 문제가 없다.
- **검토도 `state-write`에 묶는다(2026-09-01에 옮김).** 처음에는 읽기만 해서
  따로 묶었다. 그림자 추적이 붙으면서 DB에 쓰게 됐다. 손절 감시는 09:00~15:20에
  돌고 검토는 17:50이라 실제로 겹치지 않는다.

## 안 고른 쪽도 잰다 — 그림자 추적 (2026-09-01)

**검토가 낸 순위를 그대로 남겨 두고, 30일이 지나면 다시 잰다.** 그때 걸려
있던 전략과 그날 후보를 같은 종목·같은 기초설정으로 각각 돌려서 차이를
기록한다. 사람이 버튼을 눌렀든 안 눌렀든 잰다.

- **안 고른 쪽을 안 재면 기록이 한쪽만 쌓인다.** "바꿨더니 좋아졌다"는 보이고
  "안 바꿨더니 좋았다"는 안 보인다. 이 저장소는 기각된 가설을 자산으로
  취급하는데, 그 자산의 절반이 없는 셈이 된다.
- **구간마다 순위 위쪽 셋까지 남긴다.** 1위만 남기면 "2위였으면 어땠나"를
  나중에 물을 수가 없다. 그때 걸린 전략과 버튼으로 나간 후보는 순위가
  아래여도 반드시 남긴다. 견주는 짝이 둘 다 있어야 한다.
- **버튼이 안 나간 날도 잰다.** 그날 1위를 후보 자리에 놓고 견준다. 후보를
  안 낸 판단도 판단이라 같은 자로 재야 한다.
- **못 잰 것을 0%로 채우지 않는다.** 시세가 모자라 못 잰 것과 값이 안 움직인
  것은 다르다. 섞으면 신호가 실제보다 밋밋해진다. 상태를 `계산못함`으로 적는다.
- **한 건도 안 산 회차는 그렇다고 적는다.** 실제 시세로 재 보니 후보가 30일
  동안 한 종목도 안 사서 0%가 나오는 일이 흔하다. 그 0%가 "지켰다"로 읽히면
  안 된다. 화면은 수익률 밑에 매수 건수를 적고, 요약 문장도 그 회차 수를
  따로 밝힌다. 순위에서 거래 0건을 빼는 것과 같은 이유다.
- **이 숫자로 아무것도 자동으로 안 바꾼다.** 좋아 보인다고 갈아 끼우면 결국
  최근 구간에 맞추는 것이고, 그건 이 저장소가 이미 여러 번 기각한 방식이다
  (설계안 §36). 사실만 화면·시트·텔레그램에 적는다.
- **표본 20건 전에는 판단하지 않는다고 적는다.** 기간 검증의 거래 수 문턱과
  같은 값이다. 몇 건으로 "신호가 맞더라"를 말하면 그다음부터 아무도 그
  문장을 안 읽는다.
- **짝짓는 규칙이 두 곳에 있다.** `analysis/shadow.견주기()`와 `app.js`의
  `추적짝짓기`다. 갈리면 화면과 텔레그램이 서로 다른 차이를 표시한다.
  `tests/test_shadow_wiring.py`가 같은 자료를 양쪽에 넣어 고정한다.
- 남는 곳은 상태 DB `strategy_shadows`, 시트 `전략추적` 탭, 화면 전략 검토
  탭이다. 한 줄 요약은 시트 `전략검토` 탭의 `추적글` 칸에도 같이 들어간다.

## 전략을 다시 볼 때인지 알려 주는 신호 (2026-08-28)

- **검증을 돌리면 등급이 같이 나온다.** 이상없음 / 살펴볼것 / 확인필요.
  `analysis/period_check.전략신호()`가 낸다. 화면 기간 검증 탭의 표 **위에**
  뜨고, 시트 `기간검증` 탭의 `신호등급`·`신호` 칸에 쌓인다.
- **바꾸라는 말은 안 한다.** 다시 보라는 말이다. 한 구간에서 제일 좋았던
  것으로 옮기는 것이 곧 과최적화라, "무엇으로 바꿔라"에 해당하는 문장은
  아예 안 만들고 테스트가 고정한다.
- **이유가 없으면 아무 신호도 안 낸다.** 억지로 한 줄 만들면 매번 뭔가
  뜨고, 매번 뜨는 것은 진짜일 때도 안 읽힌다.
- **이상없음이 아닌 회차만 텔레그램으로 간다.** 알림을 못 보내도 검증은
  실패로 안 친다. 시트와 화면에 이미 남아 있고, 알림 하나로 워크플로를
  빨갛게 만들면 진짜 계산이 틀렸을 때의 빨간불과 구별이 안 된다.
- **n8n 시계에 토요일 09:00이 걸려 있다.** 27개를 세 구간에 견주고 신호까지
  낸다. 사람이 눌러야만 도는 점검은 결국 안 돌아서, 신호가 필요할 때 아무도
  안 보고 있게 된다.
- **처음 나온 신호는 살펴볼것이다.** 12개월 낙폭 -31.40%가 5년 낙폭
  -32.30%의 97%다. 5년 중 제일 아팠던 구간을 지금 지나고 있다. 3개월은
  27개 중 플러스가 1개뿐이라 구간 문제로 읽고 신호로 안 올렸다. 설계안 §37.
- **사람에게 가는 문장에는 줄표를 안 쓴다.** 신호글·평가글·비교총평 셋을
  훑는 테스트가 있다. 이 문장들은 화면과 텔레그램과 시트에 동시에 남아서,
  한 군데서 새면 나중에 어디를 고쳐야 하는지 알기 어렵다.

## 손절은 언제 걸리나 (2026-08-27)

- **장중에 5분마다 본다.** n8n 시계(`AutoTrading_Schedule`)의 '평일 장중
  5분마다' 노드가 `watch-stops.yml`을 부른다. 09:00~15:20이다.
  손절과 익절·트레일링만 본다. 사는 판단과 보유 기간 만료·매도 신호는
  완성된 일봉이 있어야 해서 09:05 회차 몫이다.
- **09:05 회차의 손절도 지금 값으로 잰다.** 전에는 어제 종가로 쟀다.
  전날 장중에 크게 빠진 것을 다음 날 아침에야, 그것도 전날 종가 기준으로
  판단하고 있었다. `현재가공급자`로 잔고조회의 현재가를 넘긴다.
- **성적표 숫자는 이 동작을 반영하지 않는다.** 백테스트는 일봉 종가로만
  손절을 본다. 장중 손절을 걸었다면 어땠을지는 한 번 재 봤고 평균이
  조금 나빠졌다(설계안 §32). 실거래가 더 자주 팔면 숫자가 달라진다.
- **상태 DB를 고치는 워크플로는 전부 `state-write` 그룹이다.** DB가
  드라이브의 파일 하나라 겹치면 나중 것이 앞 것을 통째로 덮는다.
  손절 감시가 5분마다 도는 뒤로 겹칠 확률이 확 올라갔다.
  `tests/test_workflow_locks.py`가 고정한다.

## 기초전략과 세부전략 (2026-08-27)

- 매매를 **사는 쪽과 파는 쪽**으로 나누고, 각 쪽을 두 층으로 본다.
  기초전략은 어떤 전략을 걸든 항상 걸리는 것이고, 세부전략은 지금 걸린 전략이다.
- **기초는 대시보드에서 바꾼다. 세부는 보기만 된다** — 전략을 바꾸는 것은
  돈이 나가는 결정이라 워크플로에서만.
- **보유 기간도 기초다.** `RiskPolicy.max_holding_days`가 0이면 전략이 정한
  대로 가고, 숫자를 넣으면 전략이 정한 기간을 덮는다. 답은 언제나
  `risk/exits.py`의 `보유상한()` 하나가 낸다 — 엔진·화면·알림 셋이 이걸 쓴다.
- **0이 뜻을 가진 칸이 넷이다.** 익절 0=끔, 보유기간 0=전략에게 맡김,
  섹터당 0=제한 없음, 거래대금 문턱 0=문턱 없음. 나머지 칸은 0을 안 받는다.
- 기초값을 바꾸면 **성적표가 지금 동작을 설명하지 않게 된다.** 성적표는
  익절 0, 보유기간은 전략이 정한 대로로 잰 숫자다. 설계안 §33·§34.

## 안전장치 — 건드리지 말 것

- `verify_kis_order.py`는 `kis_env == "real"`이면 실행을 거부한다
- `run_paper_trading.py`는 `creds.is_real`이면 종료한다
- `update_universe.py`는 `--apply` 없이는 미리보기만 한다
- `active_universe()`의 기본값은 `kind="market_cap"` — 실험용 거래대금
  스냅샷이 실거래 대상을 바꾸면 안 된다
- 킬스위치(`trading_enabled`)는 대시보드 토글과 워크플로 입력에서 켜고 끈다
