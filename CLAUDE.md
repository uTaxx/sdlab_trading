# 이 저장소에서 일할 때

## 1. 말투가 가장 중요하다

이 저장소의 주인은 트레이딩과 파이썬을 배우는 중이다. 설명이 어려우면
일이 진행되지 않는다.

### 자연스러운 한국어로 쓴다

이것이 첫 번째 규칙이다. 문장을 쓰기 전에 사람이 실제로 이렇게 말하는지
한 번 확인한다. 뜻이 통한다고 자연스러운 것은 아니다.

2026-09-01에 주인이 대화를 지우면서 그 이유를 밝혔다. 어휘가 너무 이상해서
읽기 어렵다는 것이었다. 아래는 실제로 지적받은 문장들이다.

| 쓴 것 | 고칠 것 |
|---|---|
| 실거래는 그 목록으로 안 돕니다 | 실거래는 그 목록을 쓰지 않습니다 |
| 지금 도는 매매의 성적이 아닙니다 | 지금 하는 매매의 성적이 아닙니다 |
| 서로 다른 운동장에서 돕니다 | 서로 다른 종목 목록을 봅니다 |
| 표본이 적은 자리입니다 | 표본이 적습니다 |
| 시가총액으로 무게를 주면 | 시가총액이 큰 종목에 비중을 더 두면 |
| 세 단계입니다 | 계산은 이렇게 합니다 |
| 이 숫자로 지금 무엇을 하나 | 이 값을 어디에 쓰나 |
| 줄만 세우고 거르지 않습니다 | 순서만 매기고 걸러 내지는 않습니다 |

이상한 문장이 나오는 이유는 네 가지다.

**하나, 영어를 그대로 옮긴다.** "질문이 열려 있습니다"는 open question을
직역한 것이다. 한국어로는 "아직 답을 못 드렸습니다"라고 쓴다.

**둘, 필요 없는 피동형을 쓴다.** 피동형을 쓰기 전에 능동으로 바꿔 보고,
뜻이 같으면 능동으로 쓴다. "판단됩니다"가 아니라 "판단할 수 있습니다",
"확인됩니다"가 아니라 "확인할 수 있습니다"다. 행위자가 없거나 중요하지
않을 때는 피동형이 맞다. "주문이 거절됐습니다"는 그대로 둔다.

**셋, 단어만 한글로 바꾸고 문장 구조는 영어로 둔다.** 짧은 명사구를 줄줄이
늘어놓으면 번역문처럼 읽힌다. 주어와 서술어가 있는 문장으로 쓰고, 문장끼리
"그래서", "그다음", "대신" 같은 말로 이어 준다.

**넷, 비유를 새로 만든다.** 운동장, 자리, 무게 같은 말을 끌어오지 않는다.
있는 그대로 쓴다.

### 저장소 안에서 쓰는 말을 대화에 그대로 쓰지 않는다

이 저장소는 코드와 화면에 한글 이름을 많이 쓴다. 그 이름을 사람에게 그대로
말하면 못 알아듣는다.

| 코드·화면에서 쓰는 말 | 사람에게 말할 때 |
|---|---|
| 토막 | 구간 |
| 쪼개다 | 나누다 |
| 견주다 | 비교하다 |
| 돌려 보다, 돌리다 | 실행하다, 계산하다 |
| 전략을 걸다 | 전략을 설정하다 |
| 창구 | n8n 연결 |
| 열쇠 | 인증 키 |
| 곁말 | 보조 설명 |
| 문턱 | 최소 기준 |
| 유니버스 | 매매 대상 종목 |
| 성적표 | 전략 평가 결과 |
| 기초전략 / 세부전략 | 공통 매매 규칙 / 개별 전략 |

**분석하다가 내가 새로 만든 말이 제일 위험하다.** 2026-09-02에 주인이 다시
지적했다. 저장소에 있던 말이 아니라 그날 계산하면서 내가 지어낸 말이라
사전에도 없고 화면에도 없다. 쓰기 전에 "이 말이 저장소 어딘가에 있나"를
확인하고, 없으면 풀어 쓴다.

| 그날 내가 지어낸 말 | 고칠 것 |
|---|---|
| 훑기 | 여러 설정으로 반복 계산한 것 |
| 손잡이 | 바꿔 본 설정값 |
| 칸 | 조건 하나, 또는 표의 줄 |
| 격변 / 조용 | 변동성이 큰 구간 / 작은 구간 |
| 노출 고정 | 투입 금액을 같게 맞춤 |
| 동시보유 | 동시에 들고 있는 종목 수 |
| 표본 부족 | 거래 건수가 최소 기준에 못 미침 |

### 저장소 문서를 읽어도 그 말투를 옮겨 오지 않는다

2026-09-02에 주인이 지침으로 남기라고 했다. **대화에 쓰는 말은 대화가 시작될
때의 표준 한국어 그대로 유지한다.** 세션 중에 읽은 문서에서 새 어휘를 배워
쓰지 않는다.

일을 하려면 이 저장소의 문서를 읽어야 한다. 설계안, 인수인계서, 용어 사전이
전부 위 표의 말로 쓰여 있다. 그것을 여러 개 읽고 나면 그 말이 자연스럽게
느껴져서 답에 그대로 섞여 나온다. 문서를 많이 읽은 날일수록 심해진다.

지킬 것은 셋이다.

1. **문서에서 읽은 말은 자료로만 쓰고 어휘로는 안 쓴다.** 설계안에 "토막"이라
   적혀 있어도 답에는 "구간"이라고 쓴다. 인용할 때도 마찬가지다.
2. **내가 앞서 쓴 문장을 본보기로 삼지 않는다.** 답이 길어지면 앞 문단의
   말투를 이어받게 되는데, 그러면 한 번 새어 나온 말이 끝까지 간다. 기준은
   언제나 이 절이지 방금 쓴 내 문장이 아니다.
3. **새 말을 지어내지 않는다.** 위 표의 "훑기", "손잡이"가 그렇게 나왔다.
   적당한 말이 없으면 짧게 지어내지 말고 문장으로 풀어 쓴다.
4. **이 파일의 본문도 같은 규칙을 지킨다.** 2026-09-03에 주인이 지적했다.
   "비기는 폭"이라는 말을 썼는데, 그 말이 바로 이 파일 §4에 적혀 있었다.
   읽지 말라는 말을 이 파일이 스스로 쓰고 있으면 규칙이 아무 힘이 없다.
   여기에 새 내용을 적을 때도 위 표의 왼쪽 말을 쓰지 않는다. 코드의
   이름을 가리켜야 할 때는 백틱으로 감싸고 뜻을 한 번 풀어 적는다.

**비유를 쓰지 않는다.** 지렛대, 운동장, 자리, 무게가 그런 말이다.
2026-09-03에 "지렛대"를 써서 지적받았다. "효과가 큰 쪽"이라고 쓰면 되는
자리였다. 비유는 뜻을 좁히지 않고 넓히기만 해서, 읽는 사람이 무엇을 하라는
말인지 다시 물어야 한다.

### 전문용어는 그 자리에서 풀어 쓴다

"슬리피지가 뭐야"라고 되묻게 만들지 않는 것이 이쪽 일이다.

- 처음 쓰는 용어는 한 줄로 뜻을 붙인다. "슬리피지(사겠다고 판단한 값과
  실제로 산 값의 차이)"처럼 쓴다.
- 뜻만 말고 그것을 보면 무엇을 판단하는지까지 말한다. "최대낙폭은 가장 많이
  떨어졌을 때의 하락폭"만으로는 -30%를 보고 무엇을 해야 할지 모른다.
- 숫자를 낼 때는 어느 조건에서 나온 것인지 붙인다. 대상 종목, 기간, 비용
  가정이다. 조건 없는 숫자는 나중에 검증할 수 없다.
- 영어 약어는 한글 뜻을 같이 준다. PF는 손익비, MDD는 최대낙폭이다.

용어 사전이 이미 있다. `src/muwon/dashboard/glossary.py`에 70개가 들어
있고 대시보드와 `docs/용어사전.md`가 이것을 같이 쓴다. 새 용어를 설명했으면
사전에도 넣는다. 화면과 대화가 같은 말을 쓰게 하기 위해서다.

### 보고는 결론부터 쓴다

순서는 결론, 근거, 남은 문제다.

1. **결론.** 물어본 것에 대한 답을 첫 문단에 쓴다. 한두 문장이면 된다.
2. **근거.** 그렇게 판단한 이유와 숫자. 표가 있으면 여기에 둔다.
3. **남은 문제.** 걸리는 것, 확인하지 못한 것, 다음에 할 것.

과정을 순서대로 늘어놓고 마지막에 결론을 두지 않는다. 읽는 사람은 결론을
보려고 여는 것이고, 근거는 결론에 동의가 안 될 때 본다.

### 문장 규칙

아래 문단을 본보기로 삼는다. 보고, 커밋 메시지, 사용자에게 가는 알림이
전부 이 말투다.

> 오늘 09:05 매수가 실행되지 않았습니다.
>
> 원인은 두 가지입니다. 첫째, DB에 저장해 둔 KIS 토큰이 이미 만료된
> 상태였습니다. 둘째, 그 만료 응답을 매수가능수량 0으로 잘못 읽었습니다.
> 그래서 승인된 두 종목이 모두 걸러졌습니다. 계좌 잔고는 정상이었습니다.
>
> 토큰이 만료되면 재발급 후 한 번 재시도하도록 수정했습니다. 응답 파싱도
> 함께 고쳐서, 조회에 실패한 경우와 실제로 0주인 경우를 구분합니다.
> 테스트 3건을 추가했고 전체가 통과합니다.
>
> 실제 계좌에서의 확인은 내일 09:05 실행 이후에 가능합니다.

이 문단이 지키는 것이다.

- 존댓말 평서문. 한 문장에 한 가지만 담는다.
- 결함은 결함이라고 부른다.
- 줄표(—)를 쓰지 않는다. 쉼표와 마침표로 끊는다.
- 굵게 표시를 남발하지 않는다. 중요한 것은 앞에 두는 것으로 드러낸다.
- 표는 숫자를 비교할 때만 쓴다. 설명을 표로 만들면 문장을 안 쓰게 되고,
  그러면 번역문 느낌이 더 강해진다.
- 개발 용어는 그대로 쓴다. 토큰, 파싱, 재시도, 테스트, 캐시.
- 확인하지 못한 것은 확인하지 못했다고 적는다.

**긴 답일수록 이 규칙이 먼저 무너진다.** 앞에 쓴 내 문장을 다음 문장의
본보기로 삼기 때문이다. 사고 보고서 말투로 한 번 들어가면 줄표와 비유가
쌓인다. 답이 길어진다 싶으면 이 절을 다시 읽는다.

### 화면에 나가는 글은 한 단계 더 딱딱하게 쓴다

원칙은 하나다. 쉽게 쓰되 유치하게 쓰지 않는다. 쉽게 쓰려다 구어체가 되면
돈을 다루는 화면의 신뢰가 떨어진다.

| 쓴 것 | 고칠 것 |
|---|---|
| 잽니다, 재 봅니다 | 계산합니다, 검증합니다 |
| 아무 일도 일어나지 않습니다 | 주문이 발생하지 않습니다 |
| 돈이 나가는 결정이라 | 실제 주문에 직접 영향을 주므로 |
| 한 주도 안 샀습니다 | 매수가 발생하지 않았습니다 |
| 제일 나빴던 토막 | 성과가 가장 낮은 구간 |
| 켜짐 / 꺼짐 | 동작 중 / 중지 |
| 표본이 적습니다 | 표본 부족 |

### 시장 용어는 신문 기사가 쓰는 말로 쓴다

2026-09-03에 주인이 "최대낙폭이 얕다"를 지적하면서, 화면에 쓰인 말 전체를
증시 기사와 대조해 보라고 했다. 대조해 보니 어긋난 자리가 여럿이었다.

| 쓴 것 | 고칠 것 | 까닭 |
|---|---|---|
| 최대낙폭이 얕다 / 깊다 | 최대낙폭이 작다 / 크다 | 기사는 낙폭을 크다·작다로 쓴다. 낙폭을 키웠다, 낙폭 축소 |
| 값 (주가라는 뜻) | 주가, 가격 | 기사는 값을 그 뜻으로 안 쓴다 |
| 마감 값 | 종가 | |
| 산 값 | 매수가 | |
| 사고팔린 양 | 거래량 | |
| 사고팔린 금액 | 거래대금 | |
| 흔들림 | 변동성 | |
| 번 돈 ÷ 잃은 돈 | 이익 합계 ÷ 손실 합계 | 손익비의 정의다 |
| 크게 먹는다 | 크게 번다 | |
| 값이 밀린다 | 체결가가 불리해진다 | |
| 여러 개 굴린다 | 여러 전략을 운용한다 | |

**쉽게 쓰려다 없는 말을 지어내는 것이 더 나쁘다.** 전에는 `쉬운설명`에서
종가·시가·고가·저가·거래량을 못 쓰게 막아 두었다. 그랬더니 그 자리가 "마감
값", "사고팔린 양"으로 채워졌는데, 그 말은 사전에도 기사에도 없다. 다섯
용어를 막는 목록에서 빼고 용어 사전에 뜻을 넣었다
(`tests/test_strategy_plain_words.py`의 `어려운말`).

**RSI, 과매수, 다이버전스처럼 기사에도 잘 안 나오는 말은 그대로 막는다.**
뜻을 모르면 문장 전체가 안 읽히는 말이다.

**문구를 고치기 전에 코드부터 연다.** 2026-09-01에 문구와 코드가 어긋난
곳이 열 군데 나왔다. 문구를 다듬는 일이 아니라 어긋난 곳을 찾는 일이 먼저다.
어긋나면 코드에 맞춰 문구를 고치고, 코드가 틀렸으면 그렇다고 보고한다.

**바뀔 수 있는 값을 설명문에 넣지 않는다.** 09:05, 5~15분, 20건, 5년, 2일,
30% 같은 것이다. 일정표처럼 그 값 자체가 내용인 자리는 예외다. 거래 20건
최소 기준은 `dashboard/app.js`의 `표본최소기준`과 `analysis/period_check.py`
두 곳에 있고, 어긋나면 같은 줄이 표마다 다르게 표시된다.

**보안을 과장하지 않는다.** "절대 외부로 나가지 않습니다"는 확인한 것만
쓴다. 인증 키는 조회할 때마다 서버로 보낸다. 브라우저 밖으로 안 나간다고
적으면 거짓말이다.

**투자 판단으로 읽히는 말을 쓰지 않는다.** 좋은 전략, 안전한 전략, 추천
같은 말이다. 숫자에서 바로 나오는 사실만 적는다.

**백테스트와 실제 주문과 실제 체결을 한 표에 두지 않는다.** 셋은 다른
것이고, 섞이면 번 적 없는 돈을 번 것으로 읽는다.

## 2. 사람 손을 최소화한다

**내가 확인할 수 있는 것을 사람에게 묻지 않는다.** "Published인지 봐
주세요", "Executions에 기록이 있나요", "그 값이 뭐예요"는 전부 도구로 직접
확인할 수 있다. 물어보면 그 사람은 화면을 찾아 헤매고, 찾은 뒤에도 받아
적어 나에게 전해야 한다. 그 왕복 때문에 일이 진행되지 않는다.

- **확인은 내가 한다.** n8n MCP, GitHub Actions API, 워크플로 로그, 시트,
  DB. 닿을 수 있는 곳이면 물어보지 말고 직접 본다.
- **바꾸는 것도 내가 할 수 있으면 내가 한다.** 워크플로 발행, 노드 활성화,
  설정 수정. 사람 손이 꼭 필요한 것만 남긴다.
- **남길 때는 어느 화면의 어느 버튼인지까지 적는다.** "노드를 켜세요"는
  안내가 아니다.
- **정말 사람만 할 수 있는 것은 셋뿐이다.** 비밀값 발급(토큰, API 키),
  실제 주문에 영향을 주는 결정(킬스위치를 켤지), 취향이 갈리는 선택.
- 못 하는 것을 부탁할 때는 왜 내가 못 하는지를 한 줄로 밝힌다.

## 3. 확인하지 않고 단정하지 않는다

**사실을 말하기 전에 코드나 자료를 연다.** 기억이나 추론으로 말하면
틀린다. 2026-08-31에 하루에 두 번 틀렸다.

- "실거래가 시가총액 30종목 목록을 쓴다"고 말했다. 실제로는
  `propose_buys.py`가 구글 시트의 종목 탭을 읽는다. 파일을 안 열고 말했다.
- "주문 기록이 0건"이라고 인수인계서에 썼다. 실제로는 주문 8건, 완결된
  매매 3건, 보유 3종목이 있었다. 상태 DB를 안 읽고 CLAUDE.md 문장을 그대로
  옮겼다.

둘 다 확인에 1분도 안 걸리는 것이었다.

지킬 것은 셋이다.

1. 숫자나 동작을 말하기 전에 그 값이 어디서 나오는지 연다. 설정은 시트,
   기록은 상태 DB, 동작은 스크립트다.
2. 다른 문서에 적힌 문장을 옮길 때는 지금도 맞는지 확인한다. 이 파일의
   "지금 상태"도 오래된 것일 수 있다.
3. 확인 못 한 것은 "확인하지 못했습니다"라고 적는다. 그럴듯하게 적는 것보다
   모른다고 적는 쪽이 훨씬 낫다.

**저장소를 갓 복제한 상태의 `muwon.db`는 비어 있다.** 진짜 상태 DB는 구글
드라이브에 있다. 로컬 파일을 읽고 "주문 0건"이라고 보고하면 안 된다.

## 4. 결과를 보고할 때

- **틀린 가설도 그대로 보고한다.** 이 저장소는 기각된 가설을 자산으로
  다룬다(`docs/가설기록.json`, 구글 시트). 같은 것을 두 번 시험하지 않으려면
  실패가 보여야 한다.
- 잘 안 된 것을 좋게 포장하지 않는다. "가장 나빴던 구간이 더 나빠졌다"를
  "평균은 좋아졌다"로 덮지 않는다.
- **1순위 판단 기준은 평균이 아니라 가장 나빴던 구간이다.** 평균이 아무리
  높아도 한 해에 30% 잃으면 대부분 중간에 그만두고, 그러면 평균은 받아
  보지도 못한다.
- **이 기준은 이제 문장이 아니라 값이다**(2026-09-03에 바꿈).
  `analysis/judgment.py`에 지침 목록이 있고 1~3순위를 대시보드 전략 탭 맨
  위에서 고른다. 지금 값은 가장 나빴던 구간, 최대낙폭, 누적 수익률이다.
  순위를 내는 자리는 전부 이 파일을 지나간다. 설계안 §52다.
  - **그 전까지 코드는 수익률로 정렬하고 있었다.** 적힌 것과 도는 것이
    달랐고 아무것도 빨갛게 되지 않았다. 매일 저녁 검토의 후보와 기간 검증
    순위표가 그 정렬을 썼다.
  - 지침은 두 갈래다. 계산해서 줄을 세울 수 있는 것(11개)과 사람이 확인하는
    것(8개)이다. 뒤쪽은 순위 자리에 못 들어가고 화면에 목록으로만 뜬다.
  - **지침마다 동점으로 볼 범위가 있다.** 두 전략의 점수 차이가 그 범위
    안이면 같은 것으로 보고 다음 순위로 판정한다. 범위가 없으면 0.01%p
    차이로 순위가 정해지고 2순위와 3순위는 영영 쓰이지 않는다. 코드의
    이름은 `비긴폭`인데, **사람에게 말할 때는 동점 범위라고 한다.**
  - 목록의 원본은 파이썬 하나다. 화면은 `dashboard/자료/판단지침.json`을
    읽는다. 화면에 지침 이름을 직접 적어 넣으면 저장은 되는데 순위는 안
    바뀐다. 테스트가 막는다.
  - 저장되는 곳은 시트 `설정` 탭의 `rank_1st`, `rank_2nd`, `rank_3rd`다.
    **2026-09-03에 실제 시트에서 확인했다.** 세 줄이 값과 설명까지 제대로
    들어가 있고, 값은 `worst_slice`, `max_drawdown`, `total_return`이다.
  - **2순위와 3순위는 사실상 순위를 바꾸지 않는다**(2026-09-03에 계산).
    전략 29개로 재 보니 1순위와 2순위가 매기는 순서의 상관이 0.874였고,
    2순위가 실제로 순서를 정하는 경우는 406쌍 중 17쌍(4%), 3순위는
    2쌍(0.5%)이었다. 1순위의 동점 범위가 1.0%p로 좁은 것이 원인이다.
  - **순위에서 자동으로 빼는 것은 거래 0건인 전략뿐이다.** 거래 20건 최소
    기준은 순위에 쓰이지 않고 표에 표본 부족이라고 적을 때만 쓴다. 전에는
    `enough_sample` 지침 설명이 "최소 기준에 못 미치는 전략은 순위에서
    자동으로 제외합니다"라고 적고 있었는데, 코드는 그렇게 하지 않았다.
    2026-09-03에 주인이 설명을 코드에 맞추기로 정했다. 코드를 고치는 쪽은
    저녁 검토가 내는 후보를 바꾸므로 실제 매매에 영향을 준다.
    `tests/test_judgment.py`가 설명과 코드를 묶어 둔다.

## 4-2. 화면을 고쳤으면 다른 저장소로 올려야 보인다 (2026-09-03에 알게 됨)

**여기에 커밋해도 주인이 보는 화면은 안 바뀐다.** 저장소가 둘이다.

- 매매는 `uTaxx/muwon406`의 `claude/reset-lfe6ro`에서 돈다. n8n이 부르는
  주소가 그것이다.
- 화면은 `uTaxx/sdlab_trading`의 `main`에서 GitHub Pages로 나간다.
  `deploy-dashboard.yml`이 `if: github.repository == 'uTaxx/sdlab_trading'`로
  막혀 있어서 muwon406에서는 아예 실행되지 않는다. muwon406의 Pages 자리는
  다른 프로젝트가 쓴다.

2026-09-03에 이것 때문에 **이틀치 화면 작업이 주인에게 안 보였다.** 09-01
저녁 뒤로 sdlab_trading에 아무것도 안 올라가 있었고, 판단 지침과 시장 트렌드
탭과 그림 셋이 전부 muwon406에만 쌓여 있었다. 주인이 "대시보드에 안 보여"라고
해서 알았다.

올리는 방법은 이렇다. 두 저장소는 파일이 같아야 하므로 통째로 옮긴다.

    cd /home/user/sdlab_trading && git fetch origin main
    git checkout main && git reset --hard origin/main
    git ls-files -z | xargs -0 rm -f
    git -C /home/user/muwon406 archive HEAD | tar -x -C /home/user/sdlab_trading
    git add -A && git commit && git push origin main

**화면 파일만 옮기면 배포가 막힌다.** 배포 워크플로가 올리기 전에
`scripts/export_dashboard_data.py --check`를 돌려서 `dashboard/자료`의 JSON이
파이썬 원본과 같은지 본다. 원본이 `src`에 있으므로 같이 가야 한다.

**화면을 고친 날은 마지막에 이것을 했는지 확인한다.** 안 하면 테스트도
초록불이고 커밋도 남는데 주인 화면만 옛것이다.

**올린 뒤에도 브라우저가 옛 파일을 쓸 수 있다.** 같은 날 이것도 겪었다.
올리기는 제대로 됐는데 주인 화면에서 새 탭이 빈 화면으로 나오고 판단 지침
칸이 비어 있었다. index.html은 새것을 받았고 app.js는 09-01 것이 캐시에
남아 있었다. 옛 app.js의 탭 목록에 새 탭이 없어서 화면 조각을 아예 안
펼친 것이다. 그래서 `index.html`이 `app.js?v=__배포판__`으로 부르고,
배포 워크플로가 `__배포판__`을 커밋 번호로 바꾼다. 자료 JSON도 같은
번호를 달고 간다. 치환이 한 건도 안 되면 배포가 실패한다.

## 5. 이 저장소에서 반복해서 다시 알게 되는 것들

- **로컬 venv를 쓴다.** 시스템 `python3`에는 pandas가 없다.
  `/home/user/muwon406/.venv/bin/python`을 쓴다.
- **대시보드는 브라우저로 직접 확인한다.** 테스트와 ruff를 통과하고도 화면이
  죽은 적이 여러 번 있다(같은 키의 폼 두 개, HTML 태그 노출 등).
  Playwright와 `/opt/pw-browsers/chromium`을 쓴다.
  - **킬스위치 버튼은 `confirm()`을 띄운다.** headless에서 그냥 클릭하면
    거기서 멈춘다. `page.on("dialog", ...)`로 받아 준다. 안 받으면 확인이
    통째로 시간 초과로 끝나는데, 그 실패가 "화면이 느리다"처럼 보여서 원인을
    한참 못 찾는다.
  - **화면에 뜰 수 있는 상태는 셋이다.** 진짜 자료, 아직 비었음, 아직 연결
    안 됨이다. 뒤의 둘을 같은 빈 표로 그리면 "아직 아무것도 안 샀다"와
    "산 것을 볼 수가 없다"를 구별할 수 없다. 뒤쪽이 훨씬 나쁘다.
- **폰 폭(390px)으로도 본다.** PC 폭에서만 보면 카드가 잘리고 제목이 두 줄로
  넘치는 것을 못 잡는다.
- **workflow_dispatch의 `type: boolean` 입력에 문자열을 넘기면 안 된다.**
  `"false"`가 참으로 읽혀서 진짜 올리려던 실행이 조용히 dry-run으로 끝났다
  (2026-08-26). 초록불인데 아무것도 안 올라간다. JSON 불리언 `false`를
  넘긴다. 그리고 스크립트가 어느 쪽으로 실행되는지 맨 위에 출력하게 해 뒀다.
- **셸 변수 이름에 한글을 쓰면 안 된다.** 워크플로 `run:` 안에서 `인자=""`를
  썼더니 bash가 `command not found`로 죽었다(2026-09-01). Actions 표현식만
  그런 줄 알았는데 셸도 같다. 파이썬 argparse 인자 이름은 한글이어도 된다
  (`--최소운용일`). 셸은 그것을 글자로만 넘긴다. 막히는 것은 대입문의
  왼쪽이다. `tests/test_workflow_locks.py`가 검사한다.
- **GitHub Actions 표현식에 한글 식별자를 쓰면 안 된다.** `id: 배포`와
  `steps.배포.outputs`로 워크플로가 0초 만에 죽었고 로그도 남지 않았다.
  `workflow_dispatch` 입력 이름을 `기간`으로 두었다가 dispatch가 422로
  거절됐다(2026-08-28). 파일 전체가 파싱에 실패하는 방식이라 무엇이 틀렸는지
  보이지 않는다. 식별자는 영문, 사람이 읽는 name과 description과 값은
  한글이다. `tests/test_workflow_locks.py`가 워크플로 전부를 검사한다.
- **GitHub Actions 실행 목록 응답은 토큰 한도를 넘는다.** 파일로 저장된 뒤
  python이나 json으로 파싱한다. `per_page`를 줘도 무시하고 전부 돌려준다
  (2026-08-26에 29건이 통째로 왔다). `actions_get`으로 하나씩 보는 편이 낫다.
- **실험 워크플로의 `fill_timing` 기본값은 `close`다.** 전략 평가 결과는
  `open`(실거래와 같은 규칙)으로 계산한 숫자다. 기본값으로 실행해서 평가
  결과에 넣으면 조건이 다른 숫자를 섞게 된다.
- **매매 대상 종목 스냅샷은 주마다 바뀐다.** 같은 조건으로 다시 계산해도
  숫자가 달라진다. 평가 결과에는 항상 한 실행에서 나온 숫자만 담는다.
- 실험 결과는 Actions 로그(만료됨)와 아티팩트에 남는다. 판단은
  `docs/전략엔진_설계안.md`에, 성적은 `docs/전략평가.json`에 적는다.
- **조용히 성공한 척하는 실패가 가장 비싸다.** 이 저장소에서 이미 세 번
  나왔다. `| tee`가 종료 코드를 삼킨 것, `try/except`가 텔레그램 실패를 삼킨
  것(초록불 8번, 리포트는 한 번도 안 옴), `if: always()` 업로드가 진짜 오류를
  로그 아래로 밀어낸 것이다. 새 코드를 쓸 때마다 물을 것은 하나다. 이것이
  실패하면 무엇이 빨개지나? 아무것도 안 빨개지면 실패를 숨기는 코드다.
  (`docs/사고_2026-08-25_조용한실패.md`)
- **초록불도 가끔 열어 본다.** 실패는 언젠가 보지만 "성공했는데 아무 일도
  안 한" 실행은 안 본다. 특히 오래 안 건드린 워크플로가 그렇다.
- **분봉은 그날 안에만 받을 수 있다.** 수집이 실패한 날은 그날 저녁에 손으로
  다시 실행하면 구제된다. 장 마감(15:30) 뒤에도 당일치는 나온다(16:43에
  100% 확인). 다음 날이면 영영 없다.
- **증권사 응답이 없을 때 조회는 3번까지 다시 보낸다**(2026-09-01에 추가).
  주문은 다시 보내지 않는다. 시간 초과는 주문이 접수됐는지 알 수 없는
  상태라, 다시 보내면 두 번 체결될 수 있다. `kis_client._get_통신재시도`다.

## 6. 다음에 확인할 것 (2026-09-02 저녁에 적음)

**전략 평가 결과의 숫자를 실거래의 예상치로 읽으면 안 된다.** 오늘 확인했다.
실거래가 실제로 보는 종목은 구글 시트의 63개인데 전략 평가 결과는 운영 DB
스냅샷 30개로 계산한 숫자다. 목록을 바꾸면 최대낙폭이 크게 갈린다. 거래량
급증 3일이 30종목에서 -11.47%인데 63종목에서는 -38.01%다. 순위도 바뀐다.
설계안 §44다.

**슬리피지 0.1%에서 거래가 잦은 전략이 무너진다.** 갭 상승 따라가기가 0%에서
누적 +133.5%인데 0.1%에서 +7.6%, 0.2%에서 -52.8%다. 0.1%는 큰 값이 아니다.
6만원짜리 종목의 호가 한 칸이 0.17%다. 거래가 많은 전략의 수익률을 읽을
때마다 `docs/자료/슬리피지_감도.json`을 같이 봐야 한다. 설계안 §45다.

**지금 설정된 전략이 두 가지 계산 모두에서 나쁘다.** 갭 상승 따라가기(`gap_up_go`)다.
2026-09-02 08:39에 내가 조정장을 근거로 제안했고 주인이 받아들여 바꿨는데,
그날 오후에 일곱 구간을 다시 계산해 보니 그 근거가 한 구간뿐이었다.

- 연 단위 평가(2021~2026, 실행 33607404581): 최악 연도 -23.1%, 최대낙폭
  -40.6%로 27개 중 아래에서 두 번째, 거래 1865건.
- 두 달 구간 계산(일곱 구간): 최악 -30.2%, 일곱 중 여섯 구간이 마이너스.
- 직전에 걸려 있던 변동성 돌파도 두 계산 모두에서 나쁘다(최악 -40.4%).
- 같은 두 계산에서 거래량 급증 3일(`volume_surge_3d`)이 최악 +7.4%로 1위다.
  2026년을 넣어도 1위를 지켰다(2026년 +21.9%). 최대낙폭 -10.8%도 가장 작다.

**2026-09-03 낮에 바꿨다.** 주인이 "미국 섹터 조합을 넣고 내일부터 그 전략으로
가자"고 정해서 `volume_surge_3d_us60_2`(거래량 급증 3일 + 미국 섹터 60일 상위
2개, 하루 지연)로 바꿨다. 설계안 §51이다. 지킨 순서는 이렇다. `switch-strategy.yml`의
`reserve`로 예약을 넣고, `strategy-apply.yml`을 `min_hold_days` 0으로 직접
실행해 같은 날 반영했다. 08:20 자동 반영에 맡기면 직전 반영(09-02 08:20)으로부터
30일이 안 지나서 막히기 때문이다. 텔레그램 버튼과 같은 길이라 시트 사본과
`전략변경` 탭과 알림이 따라왔다. 09-04 08:30 후보부터 새 전략으로 나온다.
그날 후보 알림의 "오늘 전략 변경" 줄은 09-03에 바꿨으므로 "변경 없음"으로
나올 수 있다. 화면 맨 위 띠와 전략 이름으로 확인한다.

**첫 며칠 확인할 것.** 이 전략은 08:30 실행에서 미국 섹터 ETF 시세를 야후에서
직접 받는다. 못 받으면 매수 신호를 안 내고 경고를 남긴다. 후보가 0개인 날은
"신호가 없어서"인지 "시세를 못 받아서"인지 로그로 구별해야 한다. 공식
평가(30종목)에서는 이 조합이 원래 전략보다 최악의 해가 낮다(+1.3% 대 +7.4%,
거래 90건). 실거래 63종목에서는 -27.4%에서 -2.6%로 올라간다. 목록에 따라
갈리는 전략이다.

### 오늘 계산해서 기각한 것 셋

전부 `docs/가설기록.json`과 설계안 §43~§45에 근거를 적어 뒀다.

- **변동성이 큰 구간에서 거래를 줄이는 것**은 근거가 없다. 투입 금액을 같게
  맞추고 다시 재니 방향이 반대였다. 일곱 구간 전부에서 여러 종목으로 나눠
  드는 쪽의 최대낙폭이 작았다.
- **변동성이 큰 구간에서 손절선을 넓히는 것**도 근거가 없다. 일곱 구간에서
  답이 갈렸고 그 갈림이 구간의 성격을 따라가지 않았다.
- **시장 지표로 매일 전략을 골라 바꾸는 것**도 기각했다. 설정값 세 벌에서
  가장 나빴던 해가 17.2%p 갈렸고, 세 벌 모두 전략 하나를 그냥 둔 것에 졌다.
  무작위로 같은 횟수만큼 바꾼 대조군을 확실히 이긴 것도 한 벌뿐이다.
  §41의 '매일 갈아타기'와 같은 결론이다.

- **장이 나쁠 때 인버스 ETF를 사는 것**도 기각했다. 기본 전략 둘 × 인버스
  1배·2배 네 조합 전부에서 현금(나쁜 날 새로 안 사기)에 졌다. 인버스 거래
  자체가 24개 설정 중 18개에서 돈을 잃었고 2배는 더 잃었다. 인버스 ETF를
  매매 대상에 넣지 않는다. 설계안 §46. **뜻밖의 것이 하나 있다.** 현금 규칙만
  얹으면 갭 상승 따라가기의 최악 해가 -19.48%에서 -4.11%로 올라갔다. 무작위
  대조군과 안 비교했으니 통과한 것이 아니라 시험할 이유가 생긴 것이다.
- **나쁜 날 새로 안 사는 현금 규칙도 기각했다**(2026-09-03). 전략 27개 중
  5개만 통과했다. 좋아 보였던 것의 상당 부분이 "덜 샀기 때문"이다. 실제 장과
  무관한 날에 안 사도 갭 상승 따라가기의 최악 해가 -19.48%에서 -12.72%로
  올라갔다. 원래 좋던 전략은 오히려 나빠졌다. 시장 지표로 매매를 켜고 끄는
  규칙은 §43, §46, §47 세 방식 전부 기각됐다. 같은 지표로 더 시험하지 않는다.
- **미국 섹터가 강하면 국내 같은 섹터 종목을 사는 규칙이 처음으로 대조군을
  이겼다**(2026-09-03, 설계안 §48). 서른 번 중 스물여덟 번이다. 가장 나빴던
  해가 -13~-20%로 국내만 보는 것(-39~-52%)보다 훨씬 낫지만 기존 최고
  전략(-7.42%)에는 못 미치고 2022년은 못 피했다. **살펴볼것이지 채택이
  아니다.** 시장 신호 규칙 네 번째 시도라 여러 번 시험하다 우연히 통과했을 가능성을
  안고 본다. 섹터 짝을 다른 ETF로 바꿔도 열두 설정 전부 이겼다(§49). 매도를 미국
  흐름으로 바꾸는 것은 방향이 없어서 국내 기준 그대로 둔다. N60 쪽이 짝과
  매도를 바꿔도 -11~-18%로 덜 흔들린다. 기존 전략 27개 위에 얹으면 16개가 나아졌다(§50). 이평+RSI 5/20이
  -7.4%에서 -0.9%로, 거래량 급증 3일이 -27.4%에서 -2.6%로. 추세를 길게 보는
  전략(골든크로스, 돈치안, 신고가)은 오히려 나빠졌다.
- **미국 섹터 따라가기를 `us_sector_follow_60_2`로 등록했다**(2026-09-03).
  공식 평가(30종목, 2021~2025)에서 최악 -6.4%, 평균 +21.0%, 낙폭 -16.6%,
  판정 조건부다. 17:50 검토가 같이 계산하고 순위에 들면 그림자 추적에 남는다.
  이 전략은 prepare()에서 미국 ETF 시세를 직접 받고, 못 받으면 매수 신호를
  안 내고 경고를 남긴다. 매매 대상에 섹터 종목이 없으면 받으러 가지 않는다
  (테스트가 네트워크에 걸리지 않게).
- **기존 규칙 위에 얹은 조합도 등록했다**(2026-09-03, `volume_surge_3d_us60_2`).
  기간 N을 20~120일, 상위 개수 k를 2~3, 지연을 0~1로 바꿔 마흔 설정을 계산했다
  (§51, `docs/자료/미국문_기간/`). 거래량 급증 3일에서는 k2 N40~N120이 전부
  이기고 N20만 나쁘다. 그 가운데인 N60, k2, 지연 1을 골랐다. 이평+RSI 5/20은
  이기는 자리가 고르지 않아 안 골랐다. 이것이 지금 실거래에 설정된 전략이다.
- **해외주식 확장은 그다음이다.** 주인이 원하는 그림은 국내·해외 주식 섞기,
  비중 조절, 장이 나쁠 때 국내외 인버스다. 인버스는 위에서 기각됐다. 남은
  순서는 미국 주식을 과거 시세로만 넣어 국내와 같이 잃는지 보는 것(2단계),
  그다음 비중 조절 규칙(3단계, 갈아타기와 같은 채점 기준), 그다음에야 해외
  주문 API다. 증권사 연결이 지금은 국내주식 API만 쓴다.

### 오늘 하다 만 것

- **n8n 장중 손절 감시가 5분마다에서 30분마다로 바뀌어 있다.** cron이
  `0,30 9-14 * * 1-5`와 `0 15 * * 1-5`다. 그런데 같은 워크플로의 안내
  메모는 아직 "장중 5분마다"라고 적혀 있다. 적힌 것과 도는 것이 다르다.
- **09-02에 11:20부터 13:00까지 손절 감시가 한 번도 안 돌았다.** 09:00부터
  11:20까지는 5분 간격으로 정확히 돌았고 그 뒤 12:00, 13:00, 13:30 세 번뿐이다.
  까닭은 확인하지 못했다. n8n 실행 목록에 실패나 취소로 남은 것이 있는지 봐야 한다.
- **로컬 PC로 옮기는 것을 검토하다 멈췄다.** 주인이 "일단 나중에"라고 했다.
  결론만 적어 둔다. 전부 옮길 수 있고 옮길 것이 생각보다 적다. 시트나
  드라이브를 만지는 파일이 `src` 안에 5개뿐이고, 텔레그램은 `getUpdates`
  롱폴링이라 공유기 포트를 열 필요가 없다. 실시간 손절 코드도 이미 있다
  (`realtime_engine.py`, `kis_websocket.py`, `run_realtime_trading.py`).
  개발 환경에서 비표준 포트가 막혀 웹소켓 실제 접속만 검증을 못 했다.
  옮길 때 지킬 것은 하나다. 켜는 날 GitHub Actions와 n8n을 같이 꺼야 한다.
  같은 계좌를 두 프로그램이 각자 판단하면 한쪽이 판 것을 다른 쪽이 다시 산다.
  주인 PC는 윈도우이고 구글 시트는 일단 유지하기로 했다.
- **평가 루프 1단계를 아직 안 만들었다.** 전략마다 점수를 날마다 계산해서
  DB와 시트에 남기되 아무것도 자동으로 바꾸지 않는 단계다. 주인이 동의했다.

### 앞서 적어 두고 아직 안 본 것

- **어제(09-01) 09:05 매수 실행이 실패로 끝났다.** 아직 안 봤다.
- **두산에너빌리티(034020)가 09-02 08:39과 09:05 사이에 보유에서 사라졌다.**
  그리고 계좌 대조에서 DB 5,856,478원 대 계좌 7,194,907원으로 1,338,429원이
  어긋난다. 까닭을 확인하지 못했다.
- **거래 기록 표의 보유기간 칸이 늘 비어 있다.** 시트에 진입일이 없다.
- **화장품 섹터를 시트에 안 올렸다.** `sector/catalog.py`에는 넣었다.
  시트에 올리면 실제 매수 대상이 바뀌므로 주인 판단이 필요하다.
- **n8n 코드 노드 수정이 MCP 승인 대기 중이다.** 그동안 전략 변경 이력의
  결과 칸이 "확인 필요"로 뜬다.
- **그림자 추적의 첫 숫자는 10월 초에 나온다.** 09-01에 남긴 12줄이 30일
  뒤에 계산된다. 그전까지 화면의 그 표는 비어 있는 것이 정상이다.

## 7. 지금 상태 (2026-09-01)

- **모의투자 자동 실행은 꺼져 있다.** `paper-trading.yml`의 schedule을 주석
  처리했고, 그 상태를 테스트가 고정하고 있다. 다시 켜면 테스트가 실패해서
  "의도한 변경인가"를 한 번 묻는다.
- **모의투자로는 이미 사고팔았다.** 2026-08-31 기준 주문 8건, 완결된 매매
  3건, 보유 3종목, 총자산 1,017만원이다. "주문 기록이 0건"이라고 적으면 안
  된다. 0건인 것은 실제 돈이 나가는 거래다.
- **슬리피지는 아직 못 쟀다.** 기준가와 체결가가 둘 다 남아 있지만, 기준가는
  전날 종가이고 체결은 다음 날 시가라 밤사이 변동과 섞여 있다. 둘을 나누려면
  주문 낸 시각의 호가를 따로 기록해야 하는데 하지 않고 있다. 지금 모든
  백테스트는 슬리피지를 0으로 놓고 계산한다.
- **테스트는 1368건이다**(2026-09-01 기준).
- 전략 27개 중 5년 내내 플러스는 다섯. 조합(AND/OR), 익절, 진입 상한은 전부
  다시 계산하고 기각했다. 지금 설정된 것은 `volume_surge_5d_ma20` 하나다.
- **매수와 매도를 따로 설정할 수 있다**(`SplitStrategy`). 조합 12개를
  계산했고 둘이 지금보다 가장 나빴던 해가 낫다. `ma_rsi_v1>volume_surge_5d_ma20`
  (최악 +10.6%, 평균은 8.9%p 낮다)과 `volume_surge_5d>macd_cross`(최악
  +4.3%, 평균 +38.0%, 대신 시간 청산이 사라진다)이다. **아직 설정하지
  않았다.** 실제 주문에 직접 영향을 주는 결정이다. 설계안 §31.

## 8. 기간별 검증 (2026-08-28)

- **대시보드 '기간 검증' 탭에서 지금 설정된 전략을 1주, 1개월, 3개월,
  12개월, 5년에 계산해 본다.** 버튼을 누르면 n8n 연결을 거쳐
  `period-check.yml`이 실행되고, 결과가 시트 `기간검증` 탭과 화면에 들어간다.
  주문은 나가지 않는다. 상태 DB도 고치지 않아서 `state-write` 그룹에 안
  묶었다.
- **1주와 1개월의 숫자는 판단에 쓸 수 없다**(2026-08-28에 추가). 1주는 거래가
  0건인 실행이 흔하고, 그럴 때 수익률 0%가 표 맨 위에 온다. 지킨 것이 아니라
  아무 일도 안 한 것이다. 전략 신호도 이 둘을 보지 않는다. 3개월, 12개월,
  5년만 읽는다. 거래 몇 건으로 신호를 내면 매주 뭔가 뜨고, 매주 뜨는 것은
  정작 중요할 때도 넘기게 된다.
- **나누는 단위는 구간 길이를 따라간다.** 1주는 날, 1개월은 주, 3개월과
  12개월은 달, 5년은 해다. 1개월을 달로 나누면 구간이 하나뿐이라 '가장
  나빴던 달'이 전체와 같아진다. 주는 ISO 주차로 센다(연말연시에 순서가 안
  꼬인다).
- **1주는 달로 안 떨어져서 `기간정의.날수`를 쓴다.** `달수`가 0이면 날수로
  거슬러 올라간다. 새 구간을 더할 때 둘 중 하나만 채운다.
- **전략 평가 결과와 계산 방법이 다르다.** 평가 결과는 해마다 따로 계산하고,
  여기서는 한 구간을 통째로 이어서 계산한다. 섞어 쓰면 안 된다.
- **처음 계산한 숫자(2026-08-28)**는 3개월 -20.74%(21건, 승률 4.8%),
  12개월 +24.90%(136건), 5년 +154.86%(674건)이다. 최대낙폭은 세 구간 모두
  -22%에서 -32% 사이다. 설계안 §35.
- **거래 20건 최소 기준은 걸러 내는 값이지 믿어도 된다는 값이 아니다.**
  이번 3개월이 21건이라 기준을 넘었지만 그것으로 판단할 숫자는 아니다.
- **화면에서 매수·매도 전략을 골라 계산할 수 있다**(2026-08-28). '지금 설정된
  것', '등록된 전략 전부(27개 비교)', 개별 전략 중에서 고른다. 결과는 시트
  `기간비교` 탭과 화면의 '골라서 계산한 것' 표에 들어간다. **여기서 고른
  것으로 매매는 바뀌지 않는다.** 전략을 실제로 바꾸는 것은 워크플로에서만
  한다.
- **`${{ github.event.inputs.* }}`를 `run:` 안에 직접 넣지 않는다.** 셸
  스크립트 글자 안으로 그대로 치환돼서 `$(...)`가 섞인 값이 명령으로
  실행된다. `env:`로 넘기고 `"$이름"`으로 쓴다. `period-check.yml`은 고쳤고,
  같은 모양이 다른 워크플로 17개에 남아 있다(`tests/test_workflow_locks.py`의
  `아직안고친것` 목록). 그 목록은 줄기만 하도록 고정해 뒀다.
- **이 비교의 절대 수익률은 믿을 수 없다.** 매매 대상이 '지금' 시가총액 상위
  30종목이라 그 목록을 5년 전으로 가져가 계산한다. 살아남은 회사만 보는
  것이라 숫자가 부풀려진다. 같은 편향이 27개 전부에 똑같이 걸리므로 순위
  비교에만 쓴다.
- **최근 3개월은 전략 문제가 아니라 구간 문제로 보인다**(2026-08-28). 27개 중
  25개가 마이너스다. 지금 설정된 것은 22위 -20.74%. 방어된 쪽은 전부 역추세
  계열이고 종목을 보유한 기간 비율이 낮다. 설계안 §36.
- **`volume_surge_3d`를 한 번 확인해 둘 것.** 5년 최대낙폭 -10.90%로 27개 중
  1위이고(지금 설정된 것은 -32.30%) 수익률은 +126.59%로 10위다. 아직
  설정하지 않았다. 실제 주문에 직접 영향을 주는 결정이고, 전략 평가 결과와
  같은 기준으로 다시 계산하는 것이 순서다.

## 9. 매일 전략 검토와 변경 승인 (2026-09-01)

원하는 구조가 두 갈래다. 하나는 매매고 하나는 전략 검토다.

    08:30 매수 후보 → 사람 승인 → 09:05 매수·매도 → 장중 손절 감시
      → 17:30 체결 정산 → 17:40 기록 → 다음 날 08:30

    17:50 전략 검토 → 텔레그램 버튼 → 사람이 한 번 누름
      → 08:20 반영 → 08:30 새 전략으로 후보 산출

**매매 갈래는 원래 다 있었다.** 손댈 것이 없다. 검토 갈래가 이번에 붙었다.

- **검토는 구간마다 후보를 따로 낸다.** 1주, 1개월, 3개월이고 셋이 다른
  전략을 가리킬 수 있다. 한 구간의 1위를 그대로 설정하면 그 구간에 맞춘 것이
  된다. 셋을 같이 보면 답이 모이는지 갈리는지 알 수 있다.
- **버튼은 한 번 누르면 예약, 같은 것을 다시 누르면 취소다**(2026-09-01에
  바꿈). 처음에는 선택과 확정 두 단계였는데 번거롭다는 지적을 받아 줄였다.
  손가락이 스치는 것에 대한 방어는 반영 시점이 대신한다. 눌러도 그날은 안
  바뀌고 다음 거래일 08:20에 반영하므로 저녁 내내 되돌릴 시간이 있다.
- **옛 메시지의 버튼도 계속 받는다.** 두 단계였을 때의 확인 버튼(`c`)과 취소
  버튼(`x`)이 대화방에 남아 있다. 확인 버튼은 지금의 누르기와 똑같이
  처리한다.
- **규칙은 `src/muwon/cloud/strategy_approval.py` 한 곳에만 있다.** 버튼을
  그리는 곳과 받는 곳과 반영하는 곳이 전부 이 파일을 지나간다. 세 곳이 각자
  DB를 만지면 규칙이 세 벌이 되고, 셋이 어긋나도 아무것도 안 빨개진다.
- **예약은 동시에 하나만 둔다.** 새로 고르면 앞의 것이 취소된다. 둘을 동시에
  예약하면 다음 날 무엇이 반영되는지 알 수 없다.
- **막힌 이유를 반드시 적는다.** 아무 말도 없으면 "볼 것이 없다"와 "계산이
  안 됐다"를 구별할 수 없다. 반영이 막히면 상태를 `막힘`으로 적고 알린다.
- **오늘 전략이 바뀌었는지는 08:30 후보 알림에 한 줄로 적는다**(2026-09-01에
  더함). 08:20 반영은 바꿨을 때와 막혔을 때만 알린다. 아무 일도 없는 날이
  대부분이라 매일 따로 보내면 알림이 흔해진다. 그렇다고 안 적으면 아침에 받은
  후보가 어느 전략으로 나온 것인지 알 수 없어서, 이미 매일 나가는 알림 안에
  넣었다. `approval.전략변경글()`이 만들고 `propose_buys.py`가 붙인다.
- **막힌 날 문장이 제일 중요하다.** 막히면 전략이 안 바뀐 채로 후보가 나온다.
  사람은 바뀐 줄 알고 승인한다. 그래서 그날은 경고 표시와 함께 "아래 후보는
  이전 전략으로 계산한 것"이라고 적는다.
- **상태 DB의 시각 칸은 UTC다.** 08:20 한국시각은 UTC로 전날 23:20이라, 날짜를
  그냥 비교하면 오늘 바꾼 것을 어제 것으로 읽는다. `오늘변경()`이 아홉 시간을
  더해서 본다. `tests/test_strategy_change_notice.py`가 고정한다.
- **사람에게 가는 글에 전략 키가 새면 안 된다.** 막힌 까닭은
  `f"...{줄.새전략}"`으로 만들어져 `volume_surge_3d`가 그대로 섞인다.
  `approval.키를이름으로()`를 거쳐 보낸다.
- **전략이 저장되는 곳과 화면이 읽는 곳이 다르다.** 실제로 쓰는 전략은 상태
  DB의 `strategy.active_keys`다. 시트 `설정` 탭의 `strategy` 칸은 그것을 옮겨
  적은 사본이고, 화면은 그 사본을 읽는다. 시트에서 그 칸을 고쳐도 매매는 안
  바뀐다.
- **버튼을 누른 시점과 시트에 반영되는 시점이 다르다.** 버튼은 예약만 적는다
  (`strategy_changes` 표). 전략은 다음 거래일 08:20에 DB에서 바뀌고, 그때
  시트 사본도 같이 고친다(2026-09-01에 더함). 전에는 사본을 17:40 기록
  저장만 고쳐서, **08:20부터 17:40까지 화면이 옛 전략을 보여 줬다.** 그 아홉
  시간이 하루 매매 전체를 덮는다. 08:30에 후보를 승인할 때 화면 맨 위 띠가
  어제 전략을 적고 있었다. 텔레그램은 DB를 읽어서 맞았고, 그래서 알림과
  화면이 서로 다른 전략을 말했다. `tests/test_strategy_apply_sheet.py`가
  고정한다.
- **화면의 자동 실행 일정 카드도 오늘 결과를 같이 적는다**(2026-09-01에 더함).
  08:20 줄 밑에 세 가지 중 하나가 붙는다. 오늘 반영함, 오늘 반영 실패, 오늘
  반영한 변경 없음이다. 일정표는 원래 무엇을 할 예정인지만 적었는데, 그러면
  전략이 바뀐 날에도 "예약이 없는 날에는 아무것도 변경하지 않습니다"만 보인다.
- **막힌 날에도 시트에 남긴다**(2026-09-01에 고침). 전에는 바꾼 날만 시트에
  남겨서 막힌 날은 화면에 아무 흔적이 없었다. 텔레그램을 놓치면 그날 무슨 일이
  있었는지 확인할 길이 없었다. `전략변경` 탭에 `상태` 칸을 더해서 반영한 날과
  막힌 날을 같이 쌓는다.
- **`상태` 칸을 못 받으면 반영했다고 적지 않는다.** n8n 연결이 그 칸을 보내기
  전 판이면 반영한 줄과 막힌 줄이 똑같이 들어온다. 그때 성공으로 읽으면 반영이
  실패한 날에 성공했다고 적는다. 모르면 "확인 필요"라고 적는다.
- **그래서 올리는 순서가 있다.** n8n 코드 노드를 먼저 고치고, 그다음에
  `apply_strategy_change.py`를 올린다. 거꾸로 하면 막힌 줄이 화면에 "확인
  필요"로 뜬다(위험한 쪽으로 틀리지는 않지만 읽는 사람이 헷갈린다).
- **어느 문장도 "바꿔라"라고 말하지 않는다.** 후보를 보여 주고 판단을 사람에게
  넘긴다. 테스트가 고정한다.
- **63종목 세 구간에 27전략이 약 3분 걸린다.** 2026-09-01 첫 실행에서 2분
  53초였다. 매일 실행하는 데 문제가 없다.
- **검토도 `state-write`에 묶는다**(2026-09-01에 옮김). 처음에는 읽기만 해서
  따로 두었다. 그림자 추적이 붙으면서 DB에 쓰게 됐다. 손절 감시는
  09:00~15:20에 실행되고 검토는 17:50이라 실제로 겹치지 않는다.

## 10. 그림자 추적, 안 고른 쪽도 계산한다 (2026-09-01)

검토가 낸 순위를 그대로 남겨 두고, 30일이 지나면 다시 계산한다. 그때 설정돼
있던 전략과 그날 후보를 같은 종목, 같은 기초설정으로 각각 계산해서 차이를
기록한다. 사람이 버튼을 눌렀든 안 눌렀든 계산한다.

- **안 고른 쪽을 안 재면 기록이 한쪽만 쌓인다.** "바꿨더니 좋아졌다"는
  보이고 "안 바꿨더니 좋았다"는 안 보인다. 이 저장소는 기각된 가설을 자산으로
  다루는데, 그 자산의 절반이 없는 셈이 된다.
- **구간마다 순위 위쪽 셋까지 남긴다.** 1위만 남기면 "2위였으면 어땠나"를
  나중에 물을 수 없다. 그때 설정된 전략과 버튼으로 나간 후보는 순위가
  아래여도 반드시 남긴다. 비교하는 짝이 둘 다 있어야 한다.
- **버튼이 안 나간 날도 계산한다.** 그날 1위를 후보 자리에 놓고 비교한다.
  후보를 안 낸 판단도 판단이라 같은 기준으로 계산해야 한다.
- **못 계산한 것을 0%로 채우지 않는다.** 시세가 모자라 못 계산한 것과 값이
  안 움직인 것은 다르다. 섞으면 신호가 실제보다 밋밋해진다. 상태를
  `계산못함`으로 적는다.
- **한 건도 안 산 실행은 그렇다고 적는다.** 실제 시세로 계산해 보니 후보가
  30일 동안 한 종목도 안 사서 0%가 나오는 일이 흔하다. 그 0%가 "지켰다"로
  읽히면 안 된다. 화면은 수익률 밑에 매수 건수를 적고, 요약 문장도 그런 실행
  수를 따로 밝힌다. 순위에서 거래 0건을 빼는 것과 같은 이유다.
- **이 숫자로 아무것도 자동으로 바꾸지 않는다.** 좋아 보인다고 갈아 끼우면
  결국 최근 구간에 맞추는 것이고, 그것은 이 저장소가 이미 여러 번 기각한
  방식이다(설계안 §36). 사실만 화면과 시트와 텔레그램에 적는다.
- **표본 20건 전에는 판단하지 않는다고 적는다.** 기간 검증의 거래 수 최소
  기준과 같은 값이다. 몇 건으로 "신호가 맞더라"를 말하면 그다음부터 아무도
  그 문장을 안 읽는다.
- **짝짓는 규칙이 두 곳에 있다.** `analysis/shadow.견주기()`와 `app.js`의
  `추적짝짓기`다. 갈리면 화면과 텔레그램이 서로 다른 차이를 표시한다.
  `tests/test_shadow_wiring.py`가 같은 자료를 양쪽에 넣어 고정한다.
- 남는 곳은 상태 DB `strategy_shadows`, 시트 `전략추적` 탭, 화면 전략 검토
  탭이다. 한 줄 요약은 시트 `전략검토` 탭의 `추적글` 칸에도 같이 들어간다.

## 10-2. 화면의 시장 트렌드 탭 (2026-09-03)

- **섹터별 등락률을 보여 주는 자리다.** 매매 대상 섹터의 지수를 동일가중으로
  만들어 5·20·60거래일 등락률을 막대그림과 표로 그린다. 코스피를 세로 점선
  기준선으로 같이 그려서, 그 선보다 오른쪽이면 시장을 이긴 것이다.
- **숫자는 08:30 실행이 만든다.** `propose_buys.py`가 섹터 지수를 이미
  만들고 있어서 그 결과를 `analysis/sector_trend.py`가 시트 `섹터트렌드`
  탭에 남긴다. 시세를 두 번 받지 않는다.
- **후보를 다 올린 뒤에 남기고 실패해도 삼킨다.** 08:30의 본래 일은 매수
  후보를 내는 것이다. 보여 주기용 표 때문에 그것이 늦으면 안 된다.
- **구간은 거래일 기준이다.** 다른 탭의 1주·1개월은 달력 기준이라 값이
  다르다. 이름에 거래일을 박아 두었다.
- **이 표로 매매하지 않는다.** 섹터 강도 상위만 사는 것은 2026-08-19에
  기각했다. 화면 맨 위에 그 사실이 적혀 있고 테스트가 고정한다.
- **막대그림은 SVG를 직접 그린다.** 차트 라이브러리를 쓰지 않는다. 테스트가
  라이브러리 이름이 들어오지 않게 막는다.
- **n8n은 읽는 길을 따로 냈다.** 기존 '화면 모양으로' 노드는 표마다 칸
  번호를 들고 있어서 통째로 다시 쓰면 관계없는 표가 조용히 망가진다.
  사본이 `n8n/대시보드자료_섹터트렌드모양으로.js`에 있고 칸 번호를 파이썬
  머리 목록과 묶어 두었다.
- **아직 실제 시트로 확인하지 못했다.** 첫 08:30 실행이 끝나야 탭이 생긴다.
  그전까지 화면은 "아직 기록이 없습니다"라고 적는다. 설계안 §53이다.

## 10-3. 화면의 판단용 그림 셋 (2026-09-03)

주인이 "전략을 전부 나에게 맡기기보다 내가 판단할 수 있는 자료를 만들자"고
해서 셋을 붙였다. 설계안 §54다.

- **자산 곡선(현재 손익 탭).** 실거래 계좌의 평가금액과 현금을 회차 순서로
  그린다. 자료는 원래 실행기록 시트에 있었고 어디에도 안 그렸을 뿐이다.
  **백테스트가 아니라고 화면에 적는다.** 가로축이 달력이 아니라 실행 회차
  순서라는 것도 적는다. 기록 없음과 한 회차뿐임을 다른 문장으로 가른다.
- **승인 되짚기(매수 승인 탭).** 승인한 종목과 거절한 종목의 5거래일과
  20거래일 뒤 등락률을 중앙값으로 견준다. `analysis/approval_review.py`다.
  기준가는 결정한 날의 **다음 거래일 시가**다. 실거래가 그때 주문한다.
  **어느 쪽이 옳다고 말하지 않는다.** 거절한 쪽에는 수수료와 슬리피지가 안
  들어 있고, 승인한 쪽의 등락률도 계속 보유했을 때의 값이라 실제 손익이
  아니다. 승인 칸이 빈 줄은 뺀다. 안 누른 것은 거절이 아니다.
- **연도 격자(백테스트 탭).** 전략 29개 × 5년을 색 격자로 그린다. 기본
  정렬은 가장 낮았던 해다. 자료는 `dashboard/자료/연도별성적.json`이고
  `scripts/build_yearly_table.py`가 만든다. **전략 평가 결과와 칸끼리 비교하면
  안 된다.** 다른 실행의 숫자이고 매매 대상 종목 스냅샷이 그 사이에 바뀌었다.
  화면이 그 경고를 표 밑에 적는다.
- 셋 중 자산 곡선과 승인 되짚기는 n8n 연결을 지나간다. **실제 시트로 확인하지
  못했다.** 연도 격자는 고정 파일이라 연결과 무관하고 브라우저에서 확인했다.

## 11. 전략을 다시 볼 때인지 알려 주는 신호 (2026-08-28)

- **검증을 실행하면 등급이 같이 나온다.** 이상없음, 살펴볼것, 확인필요다.
  `analysis/period_check.전략신호()`가 낸다. 화면 기간 검증 탭의 표 위에 뜨고,
  시트 `기간검증` 탭의 `신호등급`과 `신호` 칸에 쌓인다.
- **바꾸라는 말은 하지 않는다.** 다시 보라는 말이다. 한 구간에서 제일 좋았던
  것으로 옮기는 것이 곧 과최적화라, "무엇으로 바꿔라"에 해당하는 문장은 아예
  만들지 않고 테스트가 고정한다.
- **이유가 없으면 아무 신호도 내지 않는다.** 억지로 한 줄 만들면 매번 뭔가
  뜨고, 매번 뜨는 것은 정작 중요할 때도 넘기게 된다.
- **이상없음이 아닌 실행만 텔레그램으로 간다.** 알림을 못 보내도 검증은
  실패로 치지 않는다. 시트와 화면에 이미 남아 있고, 알림 하나로 워크플로를
  빨갛게 만들면 진짜 계산이 틀렸을 때의 빨간불과 구별할 수 없다.
- **n8n 시계에 토요일 09:00이 걸려 있다.** 27개를 세 구간에 비교하고 신호까지
  낸다. 사람이 눌러야만 실행되는 점검은 결국 실행되지 않아서, 신호가 필요할
  때 아무도 안 보고 있게 된다.
- **처음 나온 신호는 살펴볼것이다.** 12개월 최대낙폭 -31.40%가 5년 최대낙폭
  -32.30%의 97%다. 5년 중 가장 아팠던 구간을 지금 지나고 있다. 3개월은 27개
  중 플러스가 1개뿐이라 구간 문제로 보고 신호로 올리지 않았다. 설계안 §37.
- **사람에게 가는 문장에는 줄표를 쓰지 않는다.** 신호글, 평가글, 비교총평
  셋을 검사하는 테스트가 있다. 이 문장들은 화면과 텔레그램과 시트에 동시에
  남아서, 한 군데서 새면 나중에 어디를 고쳐야 하는지 알기 어렵다.

## 12. 손절은 언제 걸리나 (2026-08-27)

- **장중에 30분마다 본다**(2026-09-02에 5분에서 줄었다). n8n 시계
  (`AutoTrading_Schedule`)의 '평일 장중 30분마다' 노드가 `watch-stops.yml`을
  실행한다. cron이 `0,30 9-14`와 `0 15`라 09:00부터 30분 간격이고 마지막이
  15:00이다. **화면과 n8n 메모가 둘 다 "5분마다 09:00~15:20"이라고 적고
  있었다. 2026-09-03에 고쳤다.** 코드 주석 여러 곳에는 아직 5분마다로
  적혀 있는데, 그 주석이 말하는 것(잠금이 겹칠 확률)은 그대로 맞다. 손절과
  익절, 트레일링만 본다. 매수 판단과 보유 기간 만료, 매도 신호는 완성된
  일봉이 있어야 해서 09:05 실행이 맡는다.
- **09:05 실행의 손절도 지금 값으로 계산한다.** 전에는 어제 종가로 계산했다.
  전날 장중에 크게 빠진 것을 다음 날 아침에야, 그것도 전날 종가 기준으로
  판단하고 있었다. `현재가공급자`로 잔고조회의 현재가를 넘긴다.
- **전략 평가 결과는 이 동작을 반영하지 않는다.** 백테스트는 일봉 종가로만
  손절을 본다. 장중 손절을 넣었다면 어땠을지는 한 번 계산해 봤고 평균이 조금
  나빠졌다(설계안 §32). 실거래가 더 자주 팔면 숫자가 달라진다.
- **상태 DB를 고치는 워크플로는 전부 `state-write` 그룹이다.** DB가
  드라이브의 파일 하나라 겹치면 나중 것이 앞 것을 통째로 덮는다. 손절 감시가
  5분마다 실행되면서 겹칠 확률이 확 올라갔다.
  `tests/test_workflow_locks.py`가 고정한다.

## 13. 기초전략과 세부전략 (2026-08-27)

- 매매를 사는 쪽과 파는 쪽으로 나누고, 각 쪽을 두 층으로 본다. 기초전략은
  어떤 전략을 설정하든 항상 적용되는 것이고, 세부전략은 지금 설정된 전략이다.
- **기초는 대시보드에서 바꾼다. 세부는 보기만 된다.** 전략을 바꾸는 것은
  실제 주문에 직접 영향을 주므로 워크플로에서만 한다.
- **보유 기간도 기초다.** `RiskPolicy.max_holding_days`가 0이면 전략이 정한
  대로 가고, 숫자를 넣으면 전략이 정한 기간을 덮는다. 답은 언제나
  `risk/exits.py`의 `보유상한()` 하나가 낸다. 엔진과 화면과 알림 셋이 이것을
  쓴다.
- **0이 뜻을 가진 칸이 넷이다.** 익절 0과 보유기간 0은 전략에게 맡김,
  섹터당 0은 제한 없음, 거래대금 최소 기준 0은 기준 없음이다. 나머지 칸은
  0을 받지 않는다. 익절 0은 2026-09-02까지 "끔"이었다. 전략마다 익절선을
  다르게 두려고 뜻을 바꿨고, 지금 등록된 27개 중 익절선을 정한 것이 없어서
  결과는 아직 같다.
- 기초값을 바꾸면 **전략 평가 결과가 지금 동작을 설명하지 않게 된다.** 평가
  결과는 익절 0, 보유기간은 전략이 정한 대로로 계산한 숫자다. 설계안
  §33, §34.
- **청산은 그 종목을 산 전략을 따른다**(2026-09-02에 고침). 보유 기간,
  익절선, 매도 신호 셋 다 그렇다. 전에는 지금 설정된 전략 하나가 보유 종목
  전부의 청산을 정해서, 전략을 바꾸면 그 순간 규칙이 발밑에서 바뀌었다.
  09-02 아침에 5거래일 계획으로 들어간 종목들이 보유 상한 1거래일짜리
  전략으로 바뀌면서 45분 뒤에 한꺼번에 정리됐다. 실거래 엔진은 보유 기록의
  `strategy_key`로, 백테스트 엔진은 보유 기록에 적어 둔 전략 객체로 찾는다.
- **백테스트는 매도 대금을 판 즉시 다시 쓸 수 있다고 계산한다.** 실제로는
  2거래일 뒤에 들어온다(T+2). `BacktestEngine(결제일수=2)`로 켤 수 있고
  기본값은 0이다. 지금까지 낸 모든 숫자가 0에서 나왔다. 회전이 빠른 전략일수록
  이 차이가 유리하게 작용한다.

## 14. 안전장치. 건드리지 말 것

- `verify_kis_order.py`는 `kis_env == "real"`이면 실행을 거부한다
- `run_paper_trading.py`는 `creds.is_real`이면 종료한다
- `update_universe.py`는 `--apply` 없이는 미리보기만 한다
- `active_universe()`의 기본값은 `kind="market_cap"`이다. 실험용 거래대금
  스냅샷이 실거래 대상을 바꾸면 안 된다
- 킬스위치(`trading_enabled`)는 대시보드 토글과 워크플로 입력에서 켜고 끈다
